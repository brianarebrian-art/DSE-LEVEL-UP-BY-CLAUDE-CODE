import type { Question, Topic } from './types'
import { makeQ, topicList, rnd, type Pair, type TopicMeta, type FwMeta } from './_builder'

// HKDSE Mathematics Extended Module 1 (Calculus & Statistics) — 120 bilingual.
const q = makeQ('m1')

const T = {
  perm: { id: 'permutation_combination', zh: '排列與組合', en: 'Permutations & Combinations' },
  binomial: { id: 'binomial', zh: '二項式定理', en: 'Binomial Theorem' },
  explog: { id: 'exp_log_calculus', zh: '指數對數微積分', en: 'Exp/Log Calculus' },
  calcapp: { id: 'calculus_app', zh: '微積分應用', en: 'Applications of Calculus' },
  probdist: { id: 'probability_dist', zh: '概率分佈', en: 'Probability Distributions' },
  normal: { id: 'normal_dist', zh: '常態分佈', en: 'Normal Distribution' },
  stats: { id: 'statistics_inference', zh: '統計推斷', en: 'Statistical Inference' },
} satisfies Record<string, TopicMeta>

const FW = {
  decompose: { id: 'condition_decomposition', zh: '條件分解', en: 'Condition Decomposition', emoji: '🎯' },
  rate: { id: 'rate_intuition', zh: '變化率直覺', en: 'Rate-of-change Intuition', emoji: '📈' },
  model: { id: 'modelling', zh: '建模能力', en: 'Modelling', emoji: '🏗️' },
} satisfies Record<string, FwMeta>

const optm = (v: string): Pair => [`$${v}$`, `$${v}$`]
const opt = (v: string): Pair => [v, v]
let uid = 0
const id = (p: string) => `m1_${p}_${++uid}`
const fact = (n: number): number => n <= 1 ? 1 : n * fact(n - 1)
const nCr = (n: number, r: number) => fact(n) / (fact(r) * fact(n - r))
const nPr = (n: number, r: number) => fact(n) / fact(n - r)

// ── Permutations & combinations (24) ─────────────────────────────────────────
const perm: Question[] = []
;[[5, 2], [6, 3], [7, 2], [8, 3], [6, 2], [5, 3], [7, 4], [10, 2]].forEach(([n, r], i) => {
  const ans = nCr(n, r)
  perm.push(q(id('ncr'), T.perm, FW.decompose, i < 3 ? 'easy' : 'medium', 2019 + (i % 5), 1,
    [`計算 $C^{${n}}_{${r}}$（從 $${n}$ 個中選 $${r}$ 個，不計次序）。`, `Evaluate $C^{${n}}_{${r}}$ (choose $${r}$ from $${n}$, order ignored).`],
    [optm(`${ans}`), optm(`${nPr(n, r)}`), optm(`${ans + r}`), optm(`${ans - r}`)],
    [`$C^{${n}}_{${r}} = \\frac{${n}!}{${r}!(${n}-${r})!} = ${ans}$。`, `$C^{${n}}_{${r}} = \\frac{${n}!}{${r}!(${n}-${r})!} = ${ans}$.`]))
})
;[[5, 2], [6, 3], [7, 2], [4, 3], [6, 2], [8, 2], [5, 4], [7, 3]].forEach(([n, r], i) => {
  const ans = nPr(n, r)
  perm.push(q(id('npr'), T.perm, FW.decompose, i < 2 ? 'easy' : 'medium', 2020 + (i % 4), 1,
    [`計算 $P^{${n}}_{${r}}$（從 $${n}$ 個中排 $${r}$ 個，計次序）。`, `Evaluate $P^{${n}}_{${r}}$ (arrange $${r}$ of $${n}$, order matters).`],
    [optm(`${ans}`), optm(`${nCr(n, r)}`), optm(`${ans + n}`), optm(`${ans - n}`)],
    [`$P^{${n}}_{${r}} = \\frac{${n}!}{(${n}-${r})!} = ${ans}$。`, `$P^{${n}}_{${r}} = \\frac{${n}!}{(${n}-${r})!} = ${ans}$.`]))
})
;[[4, 24], [5, 120], [6, 720], [3, 6]].forEach(([n, ans], i) => {
  perm.push(q(id('fact'), T.perm, FW.decompose, 'easy', 2021 + (i % 3), 1,
    [`$${n}$ 個不同物件排成一行，共有多少種排法？`, `In how many ways can $${n}$ distinct objects be arranged in a row?`],
    [optm(`${ans}`), optm(`${n * n}`), optm(`${ans / n}`), optm(`${ans * 2}`)],
    [`$${n}! = ${ans}$ 種排列。`, `$${n}! = ${ans}$ arrangements.`]))
})
perm.push(
  q(id('pcc'), T.perm, FW.decompose, 'hard', 2022, 1,
    ['從 5 男 4 女中選 3 人，要求最少 1 名女性，方法數的算法是？', 'Choosing 3 from 5 men and 4 women with at least 1 woman: the count is?'],
    [optm('C^9_3 - C^5_3'), optm('C^9_3'), optm('C^4_1'), optm('C^5_3')],
    ['用補集：總數 $C^9_3$ 減去「全男」$C^5_3$，即至少 1 女。',
      'By complement: total $C^9_3$ minus all-male $C^5_3$ gives “at least 1 woman”.']),
  q(id('pcc'), T.perm, FW.decompose, 'medium', 2021, 1,
    ['「圓桌排列」$n$ 個不同的人，排法數是？', 'The number of ways to seat $n$ distinct people around a round table is?'],
    [optm('(n-1)!'), optm('n!'), optm('n^2'), optm('2n')],
    ['圓排列固定一人作參考，其餘 $(n-1)!$ 種排法。', 'Fixing one seat as reference, the rest give $(n-1)!$ circular arrangements.']),
)

// ── Binomial theorem (16) ────────────────────────────────────────────────────
const binom: Question[] = []
;[[4, 2], [5, 2], [6, 3], [5, 3], [6, 2], [7, 3]].forEach(([n, r], i) => {
  const ans = nCr(n, r)
  binom.push(q(id('bcoef'), T.binomial, FW.decompose, 'medium', 2019 + (i % 5), 1,
    [`$(1+x)^{${n}}$ 展開式中 $x^{${r}}$ 項的係數是？`, `In the expansion of $(1+x)^{${n}}$, the coefficient of $x^{${r}}$ is?`],
    [optm(`${ans}`), optm(`${nCr(n, r) + 1}`), optm(`${nCr(n, r) - 1}`), optm(`${nPr(n, r)}`)],
    [`二項式係數 $C^{${n}}_{${r}} = ${ans}$。`, `The binomial coefficient $C^{${n}}_{${r}} = ${ans}$.`]))
})
;[[3, 8], [4, 16], [5, 32], [2, 4]].forEach(([n, ans], i) => {
  binom.push(q(id('bsum'), T.binomial, FW.decompose, 'medium', 2020 + (i % 4), 1,
    [`$(1+1)^{${n}}$ 等於二項式係數之和，即？`, `$(1+1)^{${n}}$ equals the sum of the binomial coefficients, which is?`],
    [optm(`${ans}`), optm(`${ans + 1}`), optm(`${ans + 2}`), optm(`${ans - 1}`)],
    [`各係數之和 $= 2^{${n}} = ${ans}$。`, `The sum of coefficients $= 2^{${n}} = ${ans}$.`]))
})
binom.push(
  q(id('bp'), T.binomial, FW.decompose, 'hard', 2022, 1,
    ['二項式展開 $(a+b)^n$ 中，第 $(r+1)$ 項的一般式是？', 'In $(a+b)^n$, the general term (the $(r+1)$th) is?'],
    [optm('C^n_r a^{n-r} b^r'), optm('C^n_r a^r b^{n-r}'), optm('a^n + b^n'), optm('n! a b')],
    ['一般項 $T_{r+1} = C^n_r a^{n-r} b^r$。', 'The general term is $T_{r+1} = C^n_r a^{n-r} b^r$.']),
  q(id('bp'), T.binomial, FW.decompose, 'medium', 2021, 1,
    ['$(1+x)^n$ 展開式共有多少項？', 'How many terms are in the expansion of $(1+x)^n$?'],
    [optm('n+1'), optm('n'), optm('n-1'), optm('2n')],
    ['$(1+x)^n$ 展開有 $n+1$ 項（由 $x^0$ 至 $x^n$）。', '$(1+x)^n$ has $n+1$ terms (from $x^0$ to $x^n$).']),
  q(id('bp'), T.binomial, FW.decompose, 'medium', 2020, 1,
    ['$(1+x)^5$ 展開式中常數項（$x^0$）的值是？', 'The constant term ($x^0$) in $(1+x)^5$ is?'],
    [optm('1'), optm('5'), optm('10'), optm('0')],
    ['常數項即 $C^5_0 = 1$。', 'The constant term is $C^5_0 = 1$.']),
  q(id('bp'), T.binomial, FW.decompose, 'hard', 2023, 1,
    ['$(1+x)^6$ 中 $x^6$ 項的係數是？', 'The coefficient of $x^6$ in $(1+x)^6$ is?'],
    [optm('1'), optm('6'), optm('15'), optm('0')],
    ['最高次項係數 $C^6_6 = 1$。', 'The leading coefficient is $C^6_6 = 1$.']),
  q(id('bp'), T.binomial, FW.decompose, 'medium', 2019, 1,
    ['楊輝三角（帕斯卡三角）每一行的數對應？', 'Each row of Pascal’s triangle gives the?'],
    [opt('二項式係數 / binomial coefficients'), opt('質數 / prime numbers'), opt('平方數 / square numbers'), opt('斐波那契數 / Fibonacci numbers')],
    ['帕斯卡三角第 $n$ 行即 $(1+x)^n$ 的二項式係數。', 'Row $n$ of Pascal’s triangle gives the binomial coefficients of $(1+x)^n$.']),
  q(id('bp'), T.binomial, FW.decompose, 'medium', 2022, 1,
    ['二項式係數的對稱性 $C^n_r$ 等於？', 'By symmetry, the binomial coefficient $C^n_r$ equals?'],
    [optm('C^n_{n-r}'), optm('C^n_{r-1}'), optm('C^{n-1}_r'), optm('C^r_n')],
    ['對稱性：$C^n_r = C^n_{n-r}$。', 'Symmetry: $C^n_r = C^n_{n-r}$.']),
  q(id('bp'), T.binomial, FW.decompose, 'hard', 2023, 1,
    ['$(2+x)^3$ 展開式中 $x^0$（常數項）是？', 'The constant term ($x^0$) in $(2+x)^3$ is?'],
    [optm('8'), optm('1'), optm('6'), optm('12')],
    ['常數項 $= C^3_0 \\cdot 2^3 = 8$。', 'The constant term is $C^3_0 \\cdot 2^3 = 8$.']),
  q(id('bp'), T.binomial, FW.decompose, 'medium', 2021, 1,
    ['帕斯卡三角中，每個數等於其上方兩數之？', 'In Pascal’s triangle, each entry equals the ____ of the two above it?'],
    [opt('和 / sum'), opt('積 / product'), opt('差 / difference'), opt('商 / quotient')],
    ['帕斯卡三角的每個數是上方兩數之和（$C^n_r = C^{n-1}_{r-1} + C^{n-1}_r$）。',
      'Each entry is the sum of the two above it ($C^n_r = C^{n-1}_{r-1} + C^{n-1}_r$).']),
)

// ── Exp / log calculus (20) ──────────────────────────────────────────────────
const explog: Question[] = [
  q(id('el'), T.explog, FW.rate, 'easy', 2023, 1,
    ['$\\frac{d}{dx}e^x$ 等於？', 'What is $\\frac{d}{dx}e^x$?'],
    [optm('e^x'), optm('x e^{x-1}'), optm('e^{x-1}'), optm('1')],
    ['$e^x$ 的導數仍是 $e^x$。', 'The derivative of $e^x$ is $e^x$.']),
  q(id('el'), T.explog, FW.rate, 'easy', 2022, 1,
    ['$\\frac{d}{dx}\\ln x$ 等於？', 'What is $\\frac{d}{dx}\\ln x$?'],
    [optm('\\frac{1}{x}'), optm('\\ln x'), optm('x'), optm('e^x')],
    ['$\\frac{d}{dx}\\ln x = \\frac{1}{x}$。', '$\\frac{d}{dx}\\ln x = \\frac{1}{x}$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2021, 1,
    ['$\\frac{d}{dx}e^{3x}$ 等於？', 'What is $\\frac{d}{dx}e^{3x}$?'],
    [optm('3e^{3x}'), optm('e^{3x}'), optm('3x e^{3x}'), optm('e^{3}')],
    ['鏈式法則：$\\frac{d}{dx}e^{3x} = 3e^{3x}$。', 'Chain rule: $\\frac{d}{dx}e^{3x} = 3e^{3x}$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2020, 1,
    ['$\\int e^x\\,dx$ 等於？', 'What is $\\int e^x\\,dx$?'],
    [optm('e^x + C'), optm('x e^x + C'), optm('\\frac{e^x}{x} + C'), optm('e^{x+1} + C')],
    ['$\\int e^x\\,dx = e^x + C$。', '$\\int e^x\\,dx = e^x + C$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2019, 1,
    ['$\\int \\frac{1}{x}\\,dx$ 等於？', 'What is $\\int \\frac{1}{x}\\,dx$?'],
    [optm('\\ln|x| + C'), optm('-\\frac{1}{x^2} + C'), optm('\\frac{x^2}{2} + C'), optm('x + C')],
    ['$\\int \\frac{1}{x}\\,dx = \\ln|x| + C$。', '$\\int \\frac{1}{x}\\,dx = \\ln|x| + C$.']),
  q(id('el'), T.explog, FW.rate, 'hard', 2023, 1,
    ['$\\frac{d}{dx}(x e^x)$ 等於（積法則）？', 'Using the product rule, $\\frac{d}{dx}(x e^x)$ is?'],
    [optm('e^x(1+x)'), optm('e^x'), optm('x e^x'), optm('1 + e^x')],
    ['積法則：$e^x + x e^x = e^x(1+x)$。', 'Product rule: $e^x + x e^x = e^x(1+x)$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2022, 1,
    ['$\\frac{d}{dx}\\ln(2x)$ 等於？', 'What is $\\frac{d}{dx}\\ln(2x)$?'],
    [optm('\\frac{1}{x}'), optm('\\frac{2}{x}'), optm('\\frac{1}{2x}'), optm('2\\ln x')],
    ['$\\frac{d}{dx}\\ln(2x) = \\frac{2}{2x} = \\frac{1}{x}$。', '$\\frac{d}{dx}\\ln(2x) = \\frac{2}{2x} = \\frac{1}{x}$.']),
  q(id('el'), T.explog, FW.rate, 'hard', 2021, 1,
    ['$\\frac{d}{dx}e^{x^2}$ 等於？', 'What is $\\frac{d}{dx}e^{x^2}$?'],
    [optm('2x e^{x^2}'), optm('e^{x^2}'), optm('x^2 e^{x^2}'), optm('2x e^{2x}')],
    ['鏈式法則：$e^{x^2}\\cdot 2x = 2x e^{x^2}$。', 'Chain rule: $e^{x^2}\\cdot 2x = 2x e^{x^2}$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2020, 1,
    ['$\\int e^{2x}\\,dx$ 等於？', 'What is $\\int e^{2x}\\,dx$?'],
    [optm('\\frac{1}{2}e^{2x} + C'), optm('2e^{2x} + C'), optm('e^{2x} + C'), optm('\\frac{e^{2x}}{2x} + C')],
    ['$\\int e^{2x}\\,dx = \\frac{1}{2}e^{2x} + C$。', '$\\int e^{2x}\\,dx = \\frac{1}{2}e^{2x} + C$.']),
  q(id('el'), T.explog, FW.rate, 'easy', 2019, 1,
    ['$\\ln e$ 等於？', 'What is $\\ln e$?'],
    [optm('1'), optm('0'), optm('e'), optm('10')],
    ['$\\ln e = 1$（自然對數以 $e$ 為底）。', '$\\ln e = 1$ (natural log base $e$).']),
  q(id('el'), T.explog, FW.rate, 'easy', 2023, 1,
    ['$\\ln 1$ 等於？', 'What is $\\ln 1$?'],
    [optm('0'), optm('1'), optm('e'), optm('-1')],
    ['$\\ln 1 = 0$（$e^0 = 1$）。', '$\\ln 1 = 0$ (since $e^0 = 1$).']),
  q(id('el'), T.explog, FW.rate, 'medium', 2022, 1,
    ['曲線 $y = e^x$ 在 $x = 0$ 的斜率是？', 'The slope of $y = e^x$ at $x = 0$ is?'],
    [optm('1'), optm('0'), optm('e'), optm('\\infty')],
    ['$y\' = e^x$，在 $x = 0$ 時 $e^0 = 1$。', '$y\' = e^x$, and at $x = 0$ it is $e^0 = 1$.']),
  q(id('el'), T.explog, FW.rate, 'hard', 2021, 1,
    ['$\\frac{d}{dx}\\ln(x^2+1)$ 等於？', 'What is $\\frac{d}{dx}\\ln(x^2+1)$?'],
    [optm('\\frac{2x}{x^2+1}'), optm('\\frac{1}{x^2+1}'), optm('\\frac{2x}{x^2}'), optm('2x')],
    ['鏈式法則：$\\frac{1}{x^2+1}\\cdot 2x = \\frac{2x}{x^2+1}$。', 'Chain rule: $\\frac{1}{x^2+1}\\cdot 2x = \\frac{2x}{x^2+1}$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2020, 1,
    ['$\\int_0^1 e^x\\,dx$ 等於？', 'What is $\\int_0^1 e^x\\,dx$?'],
    [optm('e - 1'), optm('e'), optm('1'), optm('e + 1')],
    ['$[e^x]_0^1 = e^1 - e^0 = e - 1$。', '$[e^x]_0^1 = e^1 - e^0 = e - 1$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2019, 1,
    ['$e^{\\ln 5}$ 等於？', 'What is $e^{\\ln 5}$?'],
    [optm('5'), optm('\\ln 5'), optm('e^5'), optm('1')],
    ['$e$ 與 $\\ln$ 互為反函數，故 $e^{\\ln 5} = 5$。', '$e$ and $\\ln$ are inverses, so $e^{\\ln 5} = 5$.']),
  q(id('el'), T.explog, FW.rate, 'hard', 2023, 1,
    ['$\\frac{d}{dx}(e^x \\ln x)$ 等於？', 'What is $\\frac{d}{dx}(e^x \\ln x)$?'],
    [optm('e^x(\\ln x + \\frac{1}{x})'), optm('e^x \\ln x'), optm('\\frac{e^x}{x}'), optm('e^x + \\frac{1}{x}')],
    ['積法則：$e^x\\ln x + e^x\\cdot\\frac{1}{x} = e^x(\\ln x + \\frac{1}{x})$。',
      'Product rule: $e^x\\ln x + e^x\\cdot\\frac{1}{x} = e^x(\\ln x + \\frac{1}{x})$.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2022, 1,
    ['函數 $y = e^x$ 的特性是？', 'A key property of $y = e^x$ is that it is?'],
    [opt('永遠為正 / always positive'), opt('可為負 / can be negative'), opt('週期函數 / periodic'), opt('在 x=0 為零 / zero at x=0')],
    ['$e^x > 0$ 對所有 $x$ 成立，且單調遞增。', '$e^x > 0$ for all $x$ and is strictly increasing.']),
  q(id('el'), T.explog, FW.rate, 'medium', 2021, 1,
    ['$\\ln(ab)$ 可化簡為？', '$\\ln(ab)$ simplifies to?'],
    [optm('\\ln a + \\ln b'), optm('\\ln a \\cdot \\ln b'), optm('\\ln a - \\ln b'), optm('\\frac{\\ln a}{\\ln b}')],
    ['對數性質：$\\ln(ab) = \\ln a + \\ln b$。', 'Log law: $\\ln(ab) = \\ln a + \\ln b$.']),
  q(id('el'), T.explog, FW.rate, 'hard', 2020, 1,
    ['$y = \\ln x$ 在 $x = e$ 的斜率是？', 'The slope of $y = \\ln x$ at $x = e$ is?'],
    [optm('\\frac{1}{e}'), optm('1'), optm('e'), optm('0')],
    ['$y\' = \\frac{1}{x}$，在 $x = e$ 時為 $\\frac{1}{e}$。', '$y\' = \\frac{1}{x}$, which at $x = e$ is $\\frac{1}{e}$.']),
]

// ── Calculus applications (20) ───────────────────────────────────────────────
const calcapp: Question[] = []
;[[1, -6, 9, 3], [1, -4, 0, 2], [1, -2, -3, 1], [2, -12, 0, 3], [1, -8, 7, 4]].forEach((p, i) => {
  const [a, b, c, crit] = p // f(x)=ax^2+bx+c min at x=-b/2a
  const xmin = -b / (2 * a)
  calcapp.push(q(id('minx'), T.calcapp, FW.model, 'medium', 2019 + (i % 5), 1,
    [`函數 $f(x) = ${a === 1 ? '' : a}x^2 ${b < 0 ? '-' : '+'} ${Math.abs(b)}x ${c < 0 ? '-' : '+'} ${Math.abs(c)}$ 的極小值在 $x = ?$`,
      `The minimum of $f(x) = ${a === 1 ? '' : a}x^2 ${b < 0 ? '-' : '+'} ${Math.abs(b)}x ${c < 0 ? '-' : '+'} ${Math.abs(c)}$ occurs at $x = ?$`],
    [optm(`${xmin}`), optm(`${-xmin}`), optm(`${b}`), optm(`${xmin + 1}`)],
    [`$f'(x) = ${2 * a}x ${b < 0 ? '-' : '+'} ${Math.abs(b)} = 0 \\Rightarrow x = ${xmin}$。`,
      `$f'(x) = ${2 * a}x ${b < 0 ? '-' : '+'} ${Math.abs(b)} = 0 \\Rightarrow x = ${xmin}$.`]))
})
const calcConcept: Question[] = [
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2023, 1,
    ['函數的極值出現在導數等於多少的點？', 'A turning point of a function occurs where the derivative equals?'],
    [optm('0'), optm('1'), optm('\\infty'), optm('-1')],
    ['極值（極大/極小）出現在 $f\'(x) = 0$ 的臨界點。', 'Turning points occur at critical points where $f\'(x) = 0$.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2022, 1,
    ['若 $f\'\'(x) > 0$，該臨界點是？', 'If $f\'\'(x) > 0$ at a critical point, it is a?'],
    [opt('極小值 / minimum'), opt('極大值 / maximum'), opt('拐點 / inflection'), opt('不連續點 / discontinuity')],
    ['二階導為正表示曲線下凹向上，臨界點為極小值。', 'A positive second derivative means concave up, so the critical point is a minimum.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2021, 1,
    ['若 $f\'\'(x) < 0$，該臨界點是？', 'If $f\'\'(x) < 0$ at a critical point, it is a?'],
    [opt('極大值 / maximum'), opt('極小值 / minimum'), opt('拐點 / inflection'), opt('零點 / root')],
    ['二階導為負表示曲線上凹向下，臨界點為極大值。', 'A negative second derivative means concave down, so the point is a maximum.']),
  q(id('ca'), T.calcapp, FW.rate, 'hard', 2020, 1,
    ['導數 $\\frac{dy}{dx}$ 在應用上代表？', 'In applications, the derivative $\\frac{dy}{dx}$ represents the?'],
    [opt('瞬時變化率 / instantaneous rate of change'), opt('總面積 / total area'),
      opt('平均值 / average value'), opt('截距 / intercept')],
    ['導數是 $y$ 相對 $x$ 的瞬時變化率（曲線斜率）。', 'The derivative is the instantaneous rate of change of $y$ with respect to $x$ (the slope).']),
  q(id('ca'), T.calcapp, FW.model, 'hard', 2019, 1,
    ['定積分 $\\int_a^b f(x)\\,dx$ 在幾何上代表？', 'The definite integral $\\int_a^b f(x)\\,dx$ geometrically represents the?'],
    [opt('曲線下的面積 / area under the curve'), opt('曲線斜率 / slope of the curve'),
      opt('曲線長度 / length of the curve'), opt('最大值 / maximum value')],
    ['定積分代表 $x = a$ 至 $b$ 之間曲線與 $x$ 軸所圍的面積。', 'The definite integral is the area between the curve and the x-axis from $a$ to $b$.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2023, 1,
    ['若位移 $s(t)$，則速度 $v(t)$ 等於？', 'If displacement is $s(t)$, the velocity $v(t)$ equals?'],
    [optm('\\frac{ds}{dt}'), optm('\\int s\\,dt'), optm('s\\cdot t'), optm('\\frac{s}{t}')],
    ['速度是位移對時間的導數 $v = \\frac{ds}{dt}$。', 'Velocity is the derivative of displacement: $v = \\frac{ds}{dt}$.']),
  q(id('ca'), T.calcapp, FW.rate, 'hard', 2022, 1,
    ['若速度 $v(t)$，則加速度 $a(t)$ 等於？', 'If velocity is $v(t)$, the acceleration $a(t)$ equals?'],
    [optm('\\frac{dv}{dt}'), optm('\\int v\\,dt'), optm('v\\cdot t'), optm('\\frac{v}{t}')],
    ['加速度是速度對時間的導數 $a = \\frac{dv}{dt}$。', 'Acceleration is the derivative of velocity: $a = \\frac{dv}{dt}$.']),
  q(id('ca'), T.calcapp, FW.model, 'medium', 2021, 1,
    ['由速度求位移，需要對速度進行？', 'To find displacement from velocity, you must?'],
    [opt('積分 / integrate'), opt('微分 / differentiate'), opt('取倒數 / take the reciprocal'), opt('平方 / square it')],
    ['位移 $= \\int v\\,dt$，即對速度積分。', 'Displacement $= \\int v\\,dt$, i.e. integrate velocity.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2020, 1,
    ['當 $\\frac{dy}{dx} > 0$ 時，函數在該處？', 'Where $\\frac{dy}{dx} > 0$, the function is?'],
    [opt('遞增 / increasing'), opt('遞減 / decreasing'), opt('不變 / constant'), opt('不連續 / discontinuous')],
    ['導數為正表示函數遞增。', 'A positive derivative means the function is increasing.']),
  q(id('ca'), T.calcapp, FW.model, 'hard', 2019, 1,
    ['要使一個給定周長的矩形面積最大，矩形應為？', 'For a fixed perimeter, a rectangle of maximum area is a?'],
    [opt('正方形 / square'), opt('長條形 / long thin rectangle'), opt('任意形狀 / any shape'), opt('三角形 / triangle')],
    ['用微積分最優化可證：定周長下正方形面積最大。', 'Optimisation by calculus shows a square maximises area for a fixed perimeter.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2023, 1,
    ['$\\frac{d}{dx}(5) $（常數）等於？', 'What is $\\frac{d}{dx}(5)$ (a constant)?'],
    [optm('0'), optm('5'), optm('1'), optm('x')],
    ['常數的導數為 0。', 'The derivative of a constant is 0.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2022, 1,
    ['拐點（inflection point）是曲線凹凸性改變之處，對應？', 'An inflection point, where concavity changes, corresponds to?'],
    [optm('f\'\'(x) = 0'), optm('f\'(x) = 0'), optm('f(x) = 0'), optm('x = 0')],
    ['拐點處二階導數變號（通常 $f\'\'(x)=0$）。', 'At an inflection point the second derivative changes sign (typically $f\'\'(x)=0$).']),
  q(id('ca'), T.calcapp, FW.model, 'hard', 2021, 1,
    ['某物體高度 $h(t) = -5t^2 + 20t$，達最高點時 $t = ?$', 'A height $h(t) = -5t^2 + 20t$ reaches its maximum at $t = ?$'],
    [optm('2'), optm('4'), optm('20'), optm('-2')],
    ['$h\'(t) = -10t + 20 = 0 \\Rightarrow t = 2$。', '$h\'(t) = -10t + 20 = 0 \\Rightarrow t = 2$.']),
  q(id('ca'), T.calcapp, FW.rate, 'medium', 2020, 1,
    ['$\\int_1^3 2x\\,dx$ 等於？', 'What is $\\int_1^3 2x\\,dx$?'],
    [optm('8'), optm('6'), optm('9'), optm('4')],
    ['$[x^2]_1^3 = 9 - 1 = 8$。', '$[x^2]_1^3 = 9 - 1 = 8$.']),
  q(id('ca'), T.calcapp, FW.rate, 'easy', 2019, 1,
    ['「邊際成本」在經濟學中是總成本函數的？', 'In economics, “marginal cost” is the ______ of the total cost function.'],
    [opt('導數 / derivative'), opt('積分 / integral'), opt('平均值 / average'), opt('截距 / intercept')],
    ['邊際成本是總成本對產量的導數（每增一單位的成本）。', 'Marginal cost is the derivative of total cost with respect to quantity.']),
]

// ── Probability distributions (16) ───────────────────────────────────────────
const probdist: Question[] = []
;[[10, 0.5, 5], [20, 0.5, 10], [8, 0.25, 2], [12, 0.5, 6], [100, 0.1, 10]].forEach((p, i) => {
  const [n, prob, mean] = p
  probdist.push(q(id('bmean'), T.probdist, FW.model, 'medium', 2019 + (i % 5), 1,
    [`二項分佈 $B(${n}, ${prob})$ 的平均數（期望值）是？`, `The mean (expectation) of a binomial distribution $B(${n}, ${prob})$ is?`],
    [optm(`${mean}`), optm(`${rnd(n * prob * (1 - prob))}`), optm(`${n}`), optm(`${rnd(n + prob)}`)],
    [`$E(X) = np = ${n}\\times${prob} = ${mean}$。`, `$E(X) = np = ${n}\\times${prob} = ${mean}$.`]))
})
;[[10, 0.5, 2.5], [8, 0.5, 2], [20, 0.5, 5], [16, 0.25, 3]].forEach((p, i) => {
  const [n, prob, varr] = p
  probdist.push(q(id('bvar'), T.probdist, FW.model, 'hard', 2020 + (i % 4), 1,
    [`二項分佈 $B(${n}, ${prob})$ 的方差是？`, `The variance of a binomial distribution $B(${n}, ${prob})$ is?`],
    [optm(`${varr}`), optm(`${n * prob}`), optm(`${n}`), optm(`${rnd(varr + n)}`)],
    [`$\\text{Var}(X) = np(1-p) = ${n}\\times${prob}\\times${1 - prob} = ${varr}$。`,
      `$\\text{Var}(X) = np(1-p) = ${n}\\times${prob}\\times${1 - prob} = ${varr}$.`]))
})
probdist.push(
  q(id('pd'), T.probdist, FW.model, 'medium', 2022, 1,
    ['二項分佈適用於以下哪種情況？', 'A binomial distribution applies when there are?'],
    [opt('固定 n 次獨立試驗、每次只有成功/失敗兩種結果 / a fixed n independent trials with two outcomes'),
      opt('連續的量度 / continuous measurements'), opt('無限次試驗 / infinitely many trials'), opt('結果有三種以上 / more than two outcomes')],
    ['二項分佈：固定 $n$ 次獨立試驗、每次成功概率 $p$ 相同、結果二分。',
      'Binomial: a fixed number $n$ of independent trials, constant success probability $p$, two outcomes each.']),
  q(id('pd'), T.probdist, FW.model, 'medium', 2021, 1,
    ['泊松分佈（Poisson）常用來描述？', 'The Poisson distribution typically models?'],
    [opt('單位時間/空間內罕見事件的次數 / the count of rare events in a fixed interval'),
      opt('連續身高 / continuous heights'), opt('兩個結果 / exactly two outcomes'), opt('擲一次骰子 / one die roll')],
    ['泊松分佈描述固定區間內隨機罕見事件發生的次數（如每小時來電數）。',
      'Poisson models the number of rare random events in a fixed interval (e.g. calls per hour).']),
  q(id('pd'), T.probdist, FW.model, 'hard', 2023, 1,
    ['泊松分佈 $\\text{Po}(\\lambda)$ 的平均數與方差分別是？', 'For a Poisson distribution $\\text{Po}(\\lambda)$, the mean and variance are?'],
    [optm('\\lambda \\text{ 與 } \\lambda'), optm('\\lambda \\text{ 與 } \\lambda^2'), optm('n p \\text{ 與 } npq'), optm('0 \\text{ 與 } 1')],
    ['泊松分佈的平均數與方差都等於 $\\lambda$。', 'For a Poisson distribution both the mean and variance equal $\\lambda$.']),
  q(id('pd'), T.probdist, FW.model, 'medium', 2020, 1,
    ['隨機變量 $X$ 的期望值 $E(X)$ 代表？', 'The expectation $E(X)$ of a random variable represents the?'],
    [opt('長遠的平均值 / the long-run average'), opt('最大值 / the maximum'),
      opt('最可能的單一值 / the single most likely value only'), opt('方差 / the variance')],
    ['期望值是隨機變量取值的加權平均（長遠平均）。', 'The expectation is the probability-weighted average (long-run mean) of the variable.']),
  q(id('pd'), T.probdist, FW.model, 'medium', 2019, 1,
    ['擲一公正硬幣 10 次，「公」出現次數服從？', 'The number of heads in 10 fair coin tosses follows a?'],
    [opt('二項分佈 B(10, 0.5) / binomial B(10, 0.5)'), opt('常態分佈 / normal distribution'),
      opt('泊松分佈 / Poisson'), opt('均勻分佈 / uniform')],
    ['固定 10 次獨立試驗、每次 $p = 0.5$，服從 $B(10, 0.5)$。', 'A fixed 10 independent trials with $p = 0.5$ gives $B(10, 0.5)$.']),
  q(id('pd'), T.probdist, FW.model, 'hard', 2022, 1,
    ['所有概率分佈的總概率（所有結果概率之和）必為？', 'The total probability over all outcomes of any distribution must equal?'],
    [optm('1'), optm('0'), optm('n'), optm('100')],
    ['所有互斥且窮盡結果的概率之和為 1。', 'The probabilities of all mutually exclusive, exhaustive outcomes sum to 1.']),
  q(id('pd'), T.probdist, FW.model, 'medium', 2021, 1,
    ['當二項分佈的 $n$ 很大、$p$ 適中時，可用哪個分佈近似？', 'For large $n$ and moderate $p$, the binomial can be approximated by the?'],
    [opt('常態分佈 / normal distribution'), opt('均勻分佈 / uniform'), opt('指數分佈 / exponential'), opt('幾何分佈 / geometric')],
    ['$n$ 大時，二項分佈近似常態分佈（中央極限定理）。', 'For large $n$, the binomial is approximately normal (central limit theorem).']),
)

// ── Normal distribution (14) ─────────────────────────────────────────────────
const normal: Question[] = [
  q(id('nd'), T.normal, FW.model, 'easy', 2023, 1,
    ['標準常態分佈的平均數與標準差分別是？', 'The mean and standard deviation of the standard normal distribution are?'],
    [optm('0 \\text{ 與 } 1'), optm('1 \\text{ 與 } 0'), optm('0 \\text{ 與 } 0'), optm('1 \\text{ 與 } 1')],
    ['標準常態分佈 $N(0, 1)$：平均數 0、標準差 1。', 'The standard normal $N(0, 1)$ has mean 0 and standard deviation 1.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2022, 1,
    ['常態分佈曲線的形狀是？', 'The shape of a normal distribution curve is?'],
    [opt('對稱的鐘形 / a symmetric bell shape'), opt('均勻矩形 / a flat rectangle'),
      opt('右偏 / right-skewed'), opt('U 形 / U-shaped')],
    ['常態分佈呈對稱鐘形，平均數=中位數=眾數。', 'The normal curve is a symmetric bell, with mean = median = mode.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2021, 1,
    ['把常態變量 $X$ 標準化為 $Z$ 的公式是？', 'The formula to standardise a normal variable $X$ into $Z$ is?'],
    [optm('Z = \\frac{X - \\mu}{\\sigma}'), optm('Z = X - \\mu'), optm('Z = \\frac{X}{\\sigma}'), optm('Z = \\mu + \\sigma')],
    ['標準化：$Z = \\frac{X - \\mu}{\\sigma}$。', 'Standardise with $Z = \\frac{X - \\mu}{\\sigma}$.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2020, 1,
    ['常態分佈中，平均數兩側各 1 個標準差內約佔多少數據？', 'In a normal distribution, about what fraction of data lies within 1 SD of the mean?'],
    [optm('68\\%'), optm('95\\%'), optm('50\\%'), optm('99.7\\%')],
    ['經驗法則：±1σ ≈ 68%、±2σ ≈ 95%、±3σ ≈ 99.7%。', 'Empirical rule: ±1σ ≈ 68%, ±2σ ≈ 95%, ±3σ ≈ 99.7%.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2019, 1,
    ['常態分佈中 ±2 個標準差內約佔多少數據？', 'About what fraction lies within ±2 SD of the mean?'],
    [optm('95\\%'), optm('68\\%'), optm('99.7\\%'), optm('50\\%')],
    ['±2σ 內約佔 95%。', 'About 95% lies within ±2 SD.']),
  q(id('nd'), T.normal, FW.model, 'hard', 2023, 1,
    ['若 $X \\sim N(50, 10^2)$，則 $X = 60$ 對應的 $Z$ 值是？', 'If $X \\sim N(50, 10^2)$, the $Z$-value for $X = 60$ is?'],
    [optm('1'), optm('10'), optm('0.6'), optm('6')],
    ['$Z = \\frac{60 - 50}{10} = 1$。', '$Z = \\frac{60 - 50}{10} = 1$.']),
  q(id('nd'), T.normal, FW.model, 'hard', 2022, 1,
    ['若 $X \\sim N(100, 15^2)$，則 $X = 85$ 對應的 $Z$ 值是？', 'If $X \\sim N(100, 15^2)$, the $Z$-value for $X = 85$ is?'],
    [optm('-1'), optm('1'), optm('-15'), optm('15')],
    ['$Z = \\frac{85 - 100}{15} = -1$。', '$Z = \\frac{85 - 100}{15} = -1$.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2021, 1,
    ['標準常態分佈中，$P(Z < 0)$ 等於？', 'For the standard normal, $P(Z < 0)$ equals?'],
    [optm('0.5'), optm('0'), optm('1'), optm('0.68')],
    ['常態曲線對稱於 0，故 $P(Z < 0) = 0.5$。', 'The normal curve is symmetric about 0, so $P(Z < 0) = 0.5$.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2020, 1,
    ['常態分佈中，平均數、中位數、眾數的關係是？', 'In a normal distribution, the mean, median and mode are?'],
    [opt('三者相等 / all equal'), opt('全部不同 / all different'),
      opt('平均 > 中位 / mean > median'), opt('眾數最大 / mode is largest')],
    ['對稱鐘形使平均數 = 中位數 = 眾數。', 'The symmetric bell makes mean = median = mode.']),
  q(id('nd'), T.normal, FW.model, 'hard', 2019, 1,
    ['若 $X \\sim N(\\mu, \\sigma^2)$，標準化後 $Z$ 的分佈是？', 'If $X \\sim N(\\mu, \\sigma^2)$, the standardised $Z$ follows?'],
    [optm('N(0, 1)'), optm('N(\\mu, \\sigma)'), optm('B(n, p)'), optm('N(1, 0)')],
    ['標準化後 $Z \\sim N(0, 1)$。', 'After standardising, $Z \\sim N(0, 1)$.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2023, 1,
    ['標準常態分佈中，$P(Z > 0)$ 等於？', 'For the standard normal, $P(Z > 0)$ equals?'],
    [optm('0.5'), optm('1'), optm('0'), optm('0.95')],
    ['對稱性使 $P(Z > 0) = 0.5$。', 'By symmetry $P(Z > 0) = 0.5$.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2022, 1,
    ['常態分佈曲線下的總面積等於？', 'The total area under a normal distribution curve equals?'],
    [optm('1'), optm('0'), optm('0.5'), optm('100')],
    ['任何概率密度曲線下的總面積為 1。', 'The total area under any probability density curve is 1.']),
  q(id('nd'), T.normal, FW.model, 'hard', 2021, 1,
    ['$Z$ 值的意義是？', 'The meaning of a $Z$-value is the number of?'],
    [opt('距平均數的標準差數目 / standard deviations from the mean'), opt('原始數值 / the raw value'),
      opt('概率 / the probability'), opt('百分位 / the percentile rank')],
    ['$Z$ 值表示數值距平均數有多少個標準差。', 'A $Z$-value is how many standard deviations a value is from the mean.']),
  q(id('nd'), T.normal, FW.model, 'medium', 2020, 1,
    ['標準差越大，常態曲線會？', 'A larger standard deviation makes the normal curve?'],
    [opt('越扁平、越分散 / flatter and more spread out'), opt('越尖窄 / taller and narrower'),
      opt('不對稱 / asymmetric'), opt('不變 / unchanged')],
    ['標準差越大，數據越分散，曲線越扁平寬闊。', 'A larger SD means more spread, so the curve is flatter and wider.']),
]

// ── Statistical inference (10) ───────────────────────────────────────────────
const stats: Question[] = [
  q(id('si'), T.stats, FW.model, 'medium', 2023, 1,
    ['從總體中抽取部分個體進行研究，這部分稱為？', 'A subset of a population selected for study is called a?'],
    [opt('樣本 / sample'), opt('總體 / population'), opt('參數 / parameter'), opt('變量 / variable')],
    ['樣本是從總體抽出的部分，用以推斷總體特性。', 'A sample is a subset of the population used to infer population characteristics.']),
  q(id('si'), T.stats, FW.model, 'medium', 2022, 1,
    ['描述「總體」特性的數值（如總體平均數）稱為？', 'A numerical summary of a population (e.g. population mean) is a?'],
    [opt('參數 / parameter'), opt('統計量 / statistic'), opt('樣本 / sample'), opt('變量 / variable')],
    ['參數描述總體；統計量描述樣本（用以估計參數）。', 'A parameter describes a population; a statistic describes a sample (estimating the parameter).']),
  q(id('si'), T.stats, FW.model, 'medium', 2021, 1,
    ['「隨機抽樣」的主要目的是？', 'The main purpose of random sampling is to?'],
    [opt('減少偏差、使樣本具代表性 / reduce bias and make the sample representative'),
      opt('增加樣本數 / increase the sample size'), opt('節省時間 / save time only'), opt('保證 100% 準確 / guarantee 100% accuracy')],
    ['隨機抽樣讓每個個體有同等機會被選，減少抽樣偏差。', 'Random sampling gives each individual an equal chance, reducing selection bias.']),
  q(id('si'), T.stats, FW.model, 'hard', 2020, 1,
    ['樣本越大，樣本平均數對總體平均數的估計一般會？', 'A larger sample generally makes the sample mean’s estimate of the population mean?'],
    [opt('更可靠（標準誤差較小）/ more reliable (smaller standard error)'),
      opt('更不可靠 / less reliable'), opt('完全相同 / exactly the same'), opt('無關係 / unrelated')],
    ['樣本越大，抽樣誤差（標準誤差）越小，估計越可靠。', 'Larger samples have smaller sampling error (standard error), giving more reliable estimates.']),
  q(id('si'), T.stats, FW.model, 'medium', 2019, 1,
    ['「樣本平均數」用作估計？', 'The sample mean is used as an estimate of the?'],
    [opt('總體平均數 / population mean'), opt('總體最大值 / population maximum'),
      opt('樣本大小 / sample size'), opt('標準差為零 / zero variance')],
    ['樣本平均數是總體平均數的無偏估計量。', 'The sample mean is an unbiased estimator of the population mean.']),
  q(id('si'), T.stats, FW.model, 'medium', 2023, 1,
    ['以下哪種抽樣方法最可能引入偏差？', 'Which sampling method is most likely to introduce bias?'],
    [opt('只訪問方便接觸的人（方便抽樣）/ surveying only easily reached people (convenience)'),
      opt('簡單隨機抽樣 / simple random sampling'), opt('分層隨機抽樣 / stratified random sampling'),
      opt('系統抽樣（隨機起點）/ systematic with random start')],
    ['方便抽樣只取易接觸者，樣本不具代表性，偏差大。', 'Convenience sampling reaches only accessible people, giving an unrepresentative, biased sample.']),
  q(id('si'), T.stats, FW.model, 'hard', 2022, 1,
    ['「分層抽樣」是指？', 'Stratified sampling means?'],
    [opt('按特性分組後在各組隨機抽取 / dividing into groups, then sampling randomly within each'),
      opt('只抽第一層 / sampling only the first group'), opt('完全不隨機 / not random at all'),
      opt('每隔固定間隔抽取 / taking every kth item')],
    ['分層抽樣先按特性分層，再在各層按比例隨機抽取，提高代表性。',
      'Stratified sampling splits the population into strata, then samples each proportionally and randomly, improving representativeness.']),
  q(id('si'), T.stats, FW.model, 'medium', 2021, 1,
    ['推論統計的目標是？', 'The goal of inferential statistics is to?'],
    [opt('由樣本推斷總體 / draw conclusions about a population from a sample'),
      opt('只描述樣本 / only describe the sample'), opt('整理原始數據 / merely organise raw data'),
      opt('繪畫圖表 / only draw charts')],
    ['推論統計利用樣本資料推斷總體的特性。', 'Inferential statistics uses sample data to infer population characteristics.']),
  q(id('si'), T.stats, FW.model, 'medium', 2020, 1,
    ['「系統抽樣」是指？', 'Systematic sampling means selecting?'],
    [opt('每隔固定間隔抽取一個（隨機起點）/ every kth item from a random start'),
      opt('只抽最大值 / only the largest values'), opt('全部抽取 / all items'), opt('隨意挑選 / arbitrarily picking')],
    ['系統抽樣由隨機起點開始，每隔 $k$ 個抽一個。', 'Systematic sampling takes every kth element from a random starting point.']),
  q(id('si'), T.stats, FW.model, 'hard', 2019, 1,
    ['若樣本不具代表性，推斷結果會？', 'If a sample is unrepresentative, the inference will be?'],
    [opt('可能有偏、不可靠 / possibly biased and unreliable'), opt('一定正確 / certainly correct'),
      opt('與總體完全一致 / identical to the population'), opt('不受影響 / unaffected')],
    ['不具代表性的樣本會使推斷產生偏差，結論不可靠。', 'An unrepresentative sample biases the inference, making conclusions unreliable.']),
  q(id('si'), T.stats, FW.model, 'medium', 2023, 1,
    ['「標準誤差」隨樣本數 $n$ 增大會如何變化？', 'How does the standard error change as the sample size $n$ increases?'],
    [opt('減小（與 $\\sqrt{n}$ 成反比）/ decreases (inversely with $\\sqrt{n}$)'),
      opt('增大 / increases'), opt('不變 / stays constant'), opt('變為零 / becomes exactly zero')],
    ['標準誤差 $= \\sigma/\\sqrt{n}$，$n$ 越大誤差越小，但不會為零。',
      'The standard error is $\\sigma/\\sqrt{n}$: larger $n$ shrinks it, though never to exactly zero.']),
]

export const m1Questions: Question[] = [
  ...perm, ...binom, ...explog, ...calcapp, ...calcConcept, ...probdist, ...normal, ...stats,
]

export const m1Topics: Topic[] = topicList([
  { topic: T.perm, fw: FW.decompose, count: perm.length },
  { topic: T.binomial, fw: FW.decompose, count: binom.length },
  { topic: T.explog, fw: FW.rate, count: explog.length },
  { topic: T.calcapp, fw: FW.model, count: calcapp.length + calcConcept.length },
  { topic: T.probdist, fw: FW.model, count: probdist.length },
  { topic: T.normal, fw: FW.model, count: normal.length },
  { topic: T.stats, fw: FW.model, count: stats.length },
])
