# DSE LEVEL UP —— 未上線功能與任務 To-Do List

**生成日期：** 2026-08-09
**核查方式：** 逐項對 filesystem、live Supabase（`aegekxapxgcfdrkzisis`）、live Vercel（`prj_CQRC5oit3FL5KrrmCaAXxqUugfYu`）實物核實。無一項靠記憶或聲稱。

---

## ⚠️ 開工前必讀：兩個分類定義已按實況修正

### 一、【🔧 已寫代碼但未部署】按字面計 = **0 項**

實測三項全部對上：

| 核查點 | 實測結果 |
|---|---|
| 本機未 commit 檔案 | `git status --porcelain` → 空 |
| 未 push commit | `git log origin/main..HEAD` → 0 |
| Vercel 生產部署 | `dpl_EiumXuei9WC3DPW2P9hdRCNWvW9q` = commit `8c7da8b`（即 HEAD），state = **READY**，target = production |

**即係：冇任何一行代碼卡喺本機或 GitHub 等上線。部署管道乾淨。**

所以本清單將 🔧 重新定義為【**代碼已寫、已部署，但學生實際觸達唔到或未生效**】—— 呢個先係真正塞住嘅一格，而且的確有嘢喺入面。每項都標明「點解學生用唔到」。

### 二、L4 負責人 = **路由標籤，唔係問責簽名**

虛擬員工姓名（Victor／Amity／Max 等）只代表「呢件事應該調用邊個 skill 嘅規範同檢查清單」。

**憲章紅線：虛擬 persona 永不可以喺 `decisions.json` 簽名。** 所有題目入庫嘅實名審批位只認 Brian / Yuna。凡下表 L4 寫住虛擬員工名而任務涉及入庫，實際簽名人一定係你或 Brian —— 呢一格改唔到。

### 三、48 小時清空 P0/P1 —— 誠實講：**做唔到全部**

P0/P1 入面嘅**代碼類**任務 48 小時內可以清。但**題目審批類**任務嘅樽頸係「真人逐條批」，機器加速唔到 —— 20 條長題目人手逐條核對參考答案同評分準則，係幾個鐘嘅實工。下表已喺「預計工時」欄分開標明【機器工時】同【真人審批工時】，唔會將兩者溝埋當一件事報。

---

## 📊 總計

| 分類 | 項目總數 |
|---|---|
| 🔧 **待生效項目總數（代碼已部署，學生觸達唔到）** | **13** |
| 📝 **待開發項目總數（連代碼都未寫）** | **21** |
| **合計** | **34** |
| 其中 P0（今日必做） | 4 |
| 其中 P1（聽日必做） | 5 |
| 其中觸及憲章紅線需創辦人拍板 | 5 |

> 🔴 = 48 小時內必清（P0／P1）。Markdown 表格無法整行填色，故以 🔴 前綴 + 粗體標示。

---

# L1 戰區一：最高指揮部

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| 🔴 | **P0 今日** | **`math-long-batch-1` 10 條長題目 —— 逐條真人審批簽名** | 🔧 待生效 | **Brian（CEO）／Yuna（COO）親簽**；規範由 Victor（數學科首席）提供 | `scripts/qbank/drafts/math-long-batch-1.decisions.json` 現況 `reviewer:""`、10 條全 `pending`。完成 = reviewer 欄填真名 + 10 條逐條 approved/rejected + `promote-drafts.mjs --out math-long` + wire 入 `load.ts` 同 `index.ts` + `npm test` 綠。**驗收：喺 live 站撳「數學 → 長題目練習」真係做到題，唔再顯示「準備中」。** | 機器 0.5h ／**真人審批 2–3h** | ❌ 否（但簽名位絕不可代簽） |
| 🔴 | **P0 今日** | **`chinese-fanwen-weak-batch2` 10 條 MC —— 逐條真人審批簽名** | 🔧 待生效 | **Brian／Yuna 親簽**；規範由 Amity（中文科首席）提供 | 同上，`reviewer:""`、10 條全 `pending`。完成 = 簽名 + promote（必須用 `--out`，否則覆蓋 `chinese-reviewed.ts`）+ wire 兩處 + `npm test` 綠。 | 機器 0.5h ／**真人審批 1.5h** | ❌ 否 |
| 🔴 | **P1 聽日** | **打氣牆 1 條學生投稿已 pending 4 日 01 小時，無人審** | 🔧 待生效 | **Yuna（COO）**；情緒安全規範由 Sarah（社工）提供 | live `wall_posts` 表：`id=028f7e5a…`、`status=pending`、`created_at=2026-08-04`、`moderator_email=null`。`/wall` 同 `/admin/wall` 已上線，`WALL_ENABLED` 預設 `true`。完成 = 登入 `/admin/wall` 人手批或退，`moderator_email` 有值。**同時要定「最遲幾耐內回應」嘅營運承諾** —— 學生投稿石沉大海比冇呢個功能更傷。 | 0.25h | ❌ 否（但自動偵測自殘內容 = 已否決紅線，只可人審） |
| | P2 本週 | `CLAUDE.md` 憲章第 5 條成本數字失實（寫 HK$180.81，實際自 2026-07-30 起為 US$200） | 📝 待資源確認 | **Benjamin（CFO）** 核數 → **Yuna（COO）** 改憲章 | `/Users/yunawong0128/Downloads/CLAUDE.md:13`。完成 = 數字改為實數 + 註明生效日 + 說明「死鎖上限」係咪同步上調。憲章數字失實會令每個 session 都用錯預算前提做技術決策。 | 0.5h | ⚠️ 是 —— 憲章條文修改須雙人簽署 |
| | P2 本週 | `dse-level-up-orchestrator` skill 內文寫「162」、footer 寫「188 Skills Unified」 | 📝 待資源確認 | **Yuna（COO）** | 實測 `.claude/skills/` 目錄 = **188** 個。完成 = 內文改 188，或講清 162 係另一個數（`docs/AGENT-IDEAS-162.md` 係構想清單，唔係已安裝 skill 數）。 | 0.25h | ❌ 否 |
| | P3 本月 | 6 個 `*.review.html` 生成檔已入 git —— gitignore 決策（已問 3 次未有回覆） | 📝 待資源確認 | **Yuna（COO）** 拍板 → **Max（CTO）** 執行 | `git ls-files` 列出 6 個：`chinese-fanwen-long-batch1` / `chinese-fanwen-weak-batch2` / `chinese-p2-writing-batch1` / `econ-market-structure-mc-10` / `en-backfill-51` / `math-long-batch-1`。二選一：(A) 加入 `.gitignore` 當純生成物；(B) 保留當審批留痕。**兩個選擇都合理，但要揀一個** —— 而家係「有時 commit 有時唔 commit」嘅中間狀態。 | 0.25h | ❌ 否 |

---

# L1 戰區二：學科軍團

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| 🔴 | **P0 今日** | **108 條已上線題目掛喺未宣告 topic id —— 課題篩選器揀唔到** | 🔧 待生效 | **Kelly（QA 總監）** 統籌；各科首席確認對應 id | 實測分佈：economics 20、chemistry 29（`mole_concept`）、math 25、chinese 12、m1 10（`binomial_theorem`）、english 6、bafs 6。呢啲題**已經供應緊學生**，但因為 topic id 未喺 `*Topics` 陣列宣告，`getSubjectTopics()` 計唔到、學生喺科目頁揀課題時佢哋完全隱形。完成 = 每個 id 要麼註冊入該科 Topics 陣列（附 zh/en/framework/emoji），要麼 remap 去現有 id；`_orphan` 掃描歸零；`npm test` 綠。**參考 `docs/topic-remap-worklist.md`。** | 3–4h | ❌ 否 |
| 🔴 | **P1 聽日** | **24／25 科零書寫題 —— 理科組（物理・化學・生物）** | 📝 設計稿階段 | **Richard（物理首席）／Chloe（化學首席）／Bella（生物首席）** | 實測 `getWrittenQuestions()`：全 25 科只有 chinese 有 26 條（全部 `long`），其餘 24 科 = 0，科目頁長題目卡顯示「準備中」。管道已全通（`makeLong()` 工廠、`_gate.mjs` 雙語閘、`LongPracticeSession` runner、`LongQuestionCard` 三段摺疊全部已部署）。完成 = 每科首批 10 條結構式長題目走完整管線並上線。憲章 §12：每批 20 題、DIFF_RATIO 3:5:2。 | 機器 4h／科 ／**真人審批 3h／科** | ❌ 否 |
| 🔴 | **P1 聽日** | **24／25 科零書寫題 —— 數學組（數學・M1・M2）** | 📝 設計稿階段 | **Victor（數學科首席）** | 同上。數學組有現成 `math-long-batch-1`（10 條待簽）可作第一批，其餘 M1／M2 未有草稿。完成 = 三科各有可做嘅長題目。解析須附 Casio fx-50FH II／3650P Program 教學（憲章 §5）。 | 機器 4h／科 ／真人審批 3h／科 | ❌ 否 |
| | P2 本週 | 24／25 科零書寫題 —— 商科組（BAFS・經濟） | 📝 設計稿階段 | **Fiona（BAFS 首席）／Carson（經濟首席）** | 同上。注意 BAFS 有 `bafs-batch-2`（6 條）已喺 drafts 但**完全冇 decisions 檔**。經濟科術語紅線：Public Good 必譯「共用品」、Income Elasticity 絕對禁用。 | 機器 4h／科 ／真人審批 3h／科 | ❌ 否 |
| | P2 本週 | 24／25 科零書寫題 —— 語文組（英文・中國文學・英國文學） | 📝 設計稿階段 | **Arthur（英文首席）／Amity（中文首席）** | 同上。⚠️ 英文科**絕不可**做「Tone & Register 自動語義批改」或「嚴打 Blind Copying」—— 已第 8 次復發嘅紅線，只可做學生自我對照嘅溫和非評分提示。 | 機器 4h／科 ／真人審批 3h／科 | ⚠️ 是 —— 長答自動批改永久禁止 |
| | P3 本月 | 24／25 科零書寫題 —— 人文組（地理・歷史・中史・公民與社會發展・倫理與宗教） | 📝 待虛擬員工分配 | **George（地理首席）／Henry（歷史首席）／Henry（中史首席）／George（CSD 首席）**；倫宗科**未有指定首席** | 同上。**倫理與宗教科喺 188 個 skill 入面搵唔到對應首席** —— 要麼指派現有首席兼任，要麼呢科長題目押後。呢個係人手分配決策。 | 機器 4h／科 ／真人審批 3h／科 | ❌ 否 |
| | P3 本月 | 24／25 科零書寫題 —— 應用組（ICT・旅款・健管・設計與應用科技・音樂・體育・視藝・科技與生活） | 📝 待虛擬員工分配 | **Ian（ICT 首席）／Ian（設計與應用科技首席）**；旅款・健管・音樂・體育・視藝・科技與生活**未有指定首席** | 同上。8 科入面 6 科冇首席。ICT 長題目**依賴下方「偽代碼作答區」先做得成**（見技術裝甲師 P1）。 | 機器 4h／科 ／真人審批 3h／科 | ❌ 否 |
| | P2 本週 | 中文科實用文題型 —— 現時 0 條 | 📝 設計稿階段 | **Amity（中文科首席）** | 實測 chinese topic 分佈：`argument_essay` 得 8 條，全部議論文；實用文（書信／演講辭／建議書／通告）**一條都冇**。完成 = 新增已註冊 topic（新 topic 屬創辦人決策）+ 首批 10 條走管線上線。 | 機器 3h ／真人審批 2h | ❌ 否 |
| | P2 本週 | 中文範文薄弱篇補題（唐詩三首・詞三首） | 📝 設計稿階段 | **Amity（中文科首席）** | 實測題面出現次數：月下獨酌 **2**、登樓 **3**、聲聲慢 **3**、青玉案 **3**、論仁論孝論君子 **3**、念奴嬌 4、山居秋暝 4。相對出師表 11／岳陽樓記 11／勸學 9 明顯偏薄。完成 = 六篇每篇補到 ≥8 條。 | 機器 3h ／真人審批 2h | ❌ 否 |
| | P2 本週 | 5 個有效草稿檔完全冇 `decisions.json`（共 27 條題卡住） | 🔧 待生效 | **Kelly（QA 總監）** 統籌各科首席 | `bafs-batch-2`(6)、`econ-crossunit-batch`(5)、`english-crossunit-batch`(5)、`math-crossunit-batch`(5)、`_demo-math`(7)。呢啲題已經寫好，但連 decisions 檔都未生成，即係連「等人批」呢一步都未入到。完成 = 跑 `review-drafts.mjs` 生成審核表 → 真人逐條批 → promote。 | 機器 1h ／真人審批 3h | ❌ 否 |
| | P4 Backlog | 概念網只覆蓋中文 12 篇，其餘 24 科零概念網 | 📝 待技術評估 | **各科首席**；架構由 **Ethan（數據）** 定 | 實測 `docs/concept_network.json` v1.1：12 pieces、4 node_types、14 proven_combinations，全部中文。跨篇／跨單元組合出題現時只有中文做得到。完成 = 至少多兩科（建議經濟＋生物）有同格式概念網。 | 6h／科 | ❌ 否 |

---

# L1 戰區三：技術裝甲師

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| 🔴 | **P1 聽日** | **ICT 偽代碼／SQL／Python 作答區 —— 現時只係一個 `font-mono` textarea** | 📝 設計稿階段 | **Leo（前端）**；規範由 **Ian（ICT 首席）** 提供 | 實測 `components/LongQuestionCard.tsx:80` 只有 `font-mono` class。缺：Tab 鍵縮排保留（而家 Tab 會跳出 textarea）、語言標籤、等寬預覽、行號。ICT 學生寫唔到有縮排嘅偽代碼／Python = 呢科長題目做唔成。完成 = Tab 插入空格唔跳焦點、換行保留縮排、題目可宣告 `codeLang` 並顯示標籤。**約 2 個檔案，係所有題型任務入面最快見效嗰個。** | 3h | ❌ 否 |
| | P2 本週 | 是非判斷題型（`type: 'tf'`）—— 完全唔存在 | 📝 待技術評估 | **Max（CTO）** 定型別 → **Leo（前端）** 做卡 | 實測 `data/questions/types.ts` 只有 `MCQuestion`／`TextQuestion`／`LongQuestion`；`_gate.mjs` 亦無對應閘。你明確要求呢個題型。完成 = types.ts 加型別 + `_builder.ts` 加 `makeTF()` + `_gate.mjs` 加閘 + `promote-drafts.mjs` 支援 + 答題卡組件 + 練習 runner 分支 + 統計歸類（**是非題屬客觀題，計入準確率；長題目唔計**）。約 6–8 個檔案。 | 8h | ❌ 否 |
| | P2 本週 | BAFS 表格題（財務狀況表／損益表填格） | 📝 待技術評估 | **Max（CTO）** 評估 → **Fiona（BAFS 首席）** 定格式 | 完全冇。技術路線二選一：(A) 結構化欄位輸入（機器可比對數字，但唔算「自動批改長答」，因為係客觀數值）；(B) 純 textarea 自評。**建議 (A)** —— 財務報表格數係客觀對錯，唔屬主觀評分範疇。呢個係產品決策，唔係我可以自己揀。 | 評估 2h ／實作 10h | ⚠️ 需拍板 —— 界定「數值比對」係咪算自動批改 |
| | P3 本月 | BAFS／視藝／設計與應用科技 畫圖題 | 📝 待技術評估 | **Max（CTO）** 評估 → **Yuna（COO）** 拍板 | 完全冇。三條路：canvas 手繪、上傳相、純文字描述自評。**核心矛盾：機器永不批改 → 畫完點自評？** 冇參考圖對照就等於冇回饋。**建議先做「參考圖＋自評三級」而唔做繪圖工具** —— 學生喺紙上畫，喺屏幕對照。呢個係產品決策。 | 評估 3h ／實作 視方案 8–20h | ⚠️ 需拍板 —— 上傳相會引入用戶內容儲存，觸及 PDPO |
| | P2 本週 | `lib/sync.ts` 零測試（176 行，跨裝置進度同步） | 📝 待技術評估 | **Alan（後端）** | 實測 `lib/__tests__/` 有 7 個測試檔，冇一個測 `lib/sync.ts`。呢個模組負責 localStorage ↔ Supabase 雙向同步，錯咗會靜靜蝕學生進度。完成 = merge 邏輯、衝突處理、空狀態、壞 JSON 四類測試齊；`npm test` 由 204 增加。 | 4h | ❌ 否 |
| | P2 本週 | `dse_free_attempts_total` 死數據仍喺同步（免費化後遺留） | 🔧 待生效 | **Alan（後端）** | 實測 `lib/sync.ts` 第 4／14／50／67／84／115／137 行仍讀寫呢個 key。平台自 2026-07-12 起 100% 免費，冇任何 attempt 上限，呢個 counter 恆為死數。完成 = 移除，同時確保舊裝置 payload 有呢個欄位時唔會炸（向後相容）。 | 1.5h | ❌ 否 |
| | P3 本月 | `/api/sync/session` 路由 0 行寫入 —— 死路由 | 🔧 待生效 | **Alan（後端）** | 實測 live `user_sessions` = **0 行**，而 `app/api/sync/session/route.ts` 存在。同時 `lib/studyTime.ts:6` 明文寫住「所以⋯⋯唔使加 `user_sessions`」—— 即係設計上已經放棄咗呢張表，但路由同表都仲喺度。完成 = 二選一：接通前端寫入，或刪路由 + drop 表（要 migration）。**後果：任何「昨日／時段」統計現時計唔到。** | 2h | ❌ 否 |
| | P3 本月 | `user_progress.total_questions_done` 死欄位（136／136 行全部 = 0） | 🔧 待生效 | **Alan（後端）** | live SQL 實測：`total=136, zero_done=136, has_topic_stats=136`。真實進度全部住喺 `progress_data` JSON 嘅 `dse_topic_stats` key。呢個欄位存在但永遠 0，任何人用佢做報表都會得出「全部學生零練習」。完成 = 接通寫入，或 drop 欄位。 | 1.5h | ❌ 否 |
| | P3 本月 | `app/practice/PracticeSession.tsx` 1,079 行 | 📝 待技術評估 | **Leo（前端）** | 單一檔案過千行，改動風險高、review 困難。完成 = 拆到每檔 < 400 行，行為零改變（現有測試須全綠）。 | 6h | ❌ 否 |
| | P4 Backlog | Light-first 設計系統 Phase 2（18 個檔案） | 📝 設計稿階段 | **Kate（UI/UX）** | Phase 1 已上線（`@theme` token 化、WCAG AA 全綠）。Phase 2 餘下 18 個檔案仍有寫死色值。完成 = 全站零硬編碼 hex（導出面／hex-alpha 拼接／SVG attribute 三類刻意保留除外）+ 兩個主題都過 AA。 | 8h | ❌ 否 |
| | P3 本月 | 2 個死草稿檔要清（已刪除 agentic harness 遺留） | 🔧 待生效 | **Max（CTO）** | `agent-smoke`(1 條)、`agent-v5-batch`(6 條) 喺 `scripts/qbank/drafts/`，但對應嘅 `app/api/agent` + `lib/agents` + 三張表已於 commit `e0b6c4e` 刪除。呢啲草稿冇下游。完成 = 確認冇價值後刪檔，或明文標記為歸檔樣本。 | 0.5h | ❌ 否 |

---

# L1 戰區四：心理醫療團

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| 🔴 | **P0 今日** | **OpenDyslexic 字型檔缺失 —— 讀寫障礙模式實際冇生效** | 🔧 待生效 | **Emma（PM-UDL）** 取檔 → **Kate（UI/UX）** 驗收 | 實測 `public/fonts/` 只有 `README.md`，**冇 `OpenDyslexic-Regular.woff2`**。`app/globals.css:315` 嘅 `@font-face` 已部署，但字型檔唔存在 → 學生撳「SEN 專注模式」之後靜靜 fallback 去 Verdana，**冇任何錯誤提示**。憲章 §7 明文列 OpenDyslexic 為已上線 SEN 功能 —— 呢個係憲章同實況唔對版。完成 = 由 opendyslexic.org 下載 Regular（SIL OFL，免費可商用）→ 轉 woff2 → 命名 `OpenDyslexic-Regular.woff2` 放入 `public/fonts/` → 喺 live 站開 SEN 模式用 DevTools 確認 computed font-family 真係 OpenDyslexic。 | 1h | ❌ 否（SIL OFL 授權，零版權風險、零成本） |
| | P2 本週 | 專業求助邊界告示（產品計劃 O9.4） | 📝 設計稿階段 | **Sarah（社工）** 撰文 → **Emma（PM-UDL）** 放位 | `/relax`、`/waiting`、打氣牆三處需要一句清楚講明「本平台唔提供心理輔導，如有需要請聯絡專業服務」+ 真實熱線（**2896 0000 生命熱線／2382 0000 撒瑪利亞防止自殺會**）。⚠️ 過往規格文件多次寫錯熱線號碼（2389 2222），必須用上述已核實號碼。完成 = 三處都有靜態告示，措辭經 Sarah 審。 | 2h | ⚠️ 是 —— **絕不可加自動情緒偵測／NLP 自殺偵測**（SEN-07 已永久否決） |
| | P2 本週 | 打氣牆危機內容處理流程（產品計劃 O7.3 之**紅線改造版**） | 📝 待資源確認 | **Sarah（社工）** 定流程 → **Yuna（COO）** 拍板 | ⚠️ **原規格要求「自動偵測危機內容」= 已否決紅線。** 可以做嘅版本：(1) `/admin/wall` 審核介面固定顯示求助熱線同處理指引，畀人審時參考；(2) 定明「幾耐內必審」嘅營運承諾；(3) 投稿頁靜態顯示熱線。完成 = 三項落地，且**零自動判斷、零心理健康 PII 入庫**。 | 3h | ⚠️ 是 —— 自動偵測永久禁止，只可做人審輔助 |
| | P3 本月 | 減壓區數據隔離建置檢查（產品計劃 O8.3） | 📝 待技術評估 | **Max（CTO）** 查 → **Emma（PM-UDL）** 驗收 | 確認 `/relax` 系列（呼吸／grounding／虛擬超市／solo／group）冇將任何情緒狀態寫入 Supabase。完成 = 出一份實測報告，逐個路由列明寫入目標；如發現有寫入 server，即改為純 localStorage。 | 2h | ⚠️ 是 —— 心理健康 PII 不得入庫 |
| | P3 本月 | 60 秒逆向鎖死擴展至中難度題（產品計劃 O2.1） | 📝 設計稿階段 | **Emma（PM-UDL）** 定閾值 → **Leo（前端）** 實作 | 現時只有中高難度觸發。O2.1 建議放寬至 medium。**風險：觸發過密會由「幫助」變成「懲罰」，直接撞憲章 §7 大愛紅線。** 完成 = 加設每日觸發上限 + 學生可跳過 + 情緒溫度計已有嘅「😰 跳過去 /relax」路徑保持可用；上線後觀察一週。**產品計劃列為 Week 1 首選（MVP 半日、回滾成本最低）。** | 4h | ⚠️ 是 —— 需確保唔會變成懲罰機制 |

---

# L1 戰區五：滲透情報局

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| | P3 本月 | 2026 年考評局課程修訂追蹤（已知 4 科有修訂） | 📝 待資源確認 | **David（考評局動態監察）** | `docs/dse-syllabus-sources.md` 已記錄已核實嘅 HKEAA／EDB 來源。完成 = 逐科比對現有題庫涵蓋範圍 vs 2026 修訂版課程，列出需增／需刪嘅課題。憲章要求考評局變更 48 小時內提報。 | 4h | ❌ 否 |
| | P4 Backlog | 錯題 DNA 上雲（產品計劃 O5.1） | 📝 待技術評估 | **Ethan（數據）** 設計 → **Alan（後端）** 實作 → **Benjamin（CFO）** 核成本 | 現時錯題 DNA 純 localStorage，換裝置即失。建議 `user_error_dna` 聚合表（唔存個別答題記錄）。成本估算：約 40,000 calls／1,000 MAU = Supabase 免費額 4%。⚠️ **必須先解決「未登入學生點算」** —— 全站主打免登入可用，強制登入會撞平權原則。 | 評估 3h ／實作 10h | ⚠️ 需拍板 —— 唔可以令功能變相強制登入 |
| | P4 Backlog | 框架熱力圖（產品計劃 N1.1） | 📝 待技術評估 | **Ethan（數據）** | ⚠️ **產品計劃建議用經濟科試點，實測唔適合** —— economics 嘅 `quantitative:115` 係題型唔係概念、`macro:3` vs `macro_modelling:29` 未合併、`reviewed:20` 係來源標籤唔係框架。**建議改用中文＋生物試點**（框架分類乾淨）。完成 = 純 SVG + Tailwind 熱力圖（禁 Chart.js／D3／Recharts），兩科可見。 | 8h | ❌ 否（純 SVG，零新套件） |

---

# L1 戰區六：後勤要塞

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| 🔴 | **P1 聽日** | **`WALL_SALT` 喺 Vercel 有冇設 —— 未能核實** | 🔧 待生效 | **你（Yuna）親自查**；技術說明由 **Max（CTO）** 提供 | `lib/wall/identity.ts:18` 嘅 fallback 鏈係 `WALL_SALT ?? AUTH_SECRET ?? 'shadow-study-room'`。**如果 Vercel 冇設 `WALL_SALT`，代碼唔會報錯，會靜靜用第二或第三個值** —— 第三個係硬編碼公開字串，即匿名 hash 可被任何人重算，打氣牆匿名性失效。**我冇工具讀得到 Vercel env，呢項只有你查得到。** 完成 = 喺 Vercel dashboard 確認 `WALL_SALT` 存在；如冇，用 `openssl rand -base64 33` 自行生成並**直接貼入 Vercel**（切勿貼入對話或寫入檔案）。 | 0.25h | ⚠️ 是 —— secret 絕不可經對話傳遞 |
| | P2 本週 | `rls_auto_enable()` SECURITY DEFINER + anon 可呼叫，從未稽核 | 📝 待技術評估 | **Max（CTO）** | 呢個函式以 definer 權限執行且匿名可調用，屬高風險面。完成 = 讀函式定義、確認佢實際做咩、決定收窄 `REVOKE EXECUTE FROM anon` 抑或保留（附理由）。 | 2h | ❌ 否 |
| | P3 本月 | 全庫術語二次掃描（EDB《常用英漢辭彙》對齊） | 📝 待資源確認 | **Oscar（編輯）／Kelly（QA）** | `docs/TERMINOLOGY.md` 已確立 EDB 辭彙表為權威，並記錄咗「兩個官方譯法並存、不可統一」嘅陷阱（比熱容量／比熱容、限制因素／限制因子）。完成 = 對 5,201 條題目跑一次全庫掃描，列出所有候選不一致 + **逐個查證先改**（唔可以見唔同就改）。 | 4h | ⚠️ 是 —— 誤「統一」並存譯法會製造 40+ 條假錯誤 |
| | P4 Backlog | `npm run qa` 三閘之外，加 orphan-topic 閘 | 📝 待技術評估 | **Kelly（QA）** | 上方 108 條孤兒課題係人手掃先發現。完成 = 加入 CI 閘：任何題目 topic 唔喺該科 Topics 陣列即紅測試。**⚠️ 依憲章 §6，加新閘前必須先統計現有幾多條會 fail（= 108）+ 打印 10 個例子 + 附 migration plan** —— 即係要先清完孤兒先加閘，否則即刻紅。 | 2h（清完孤兒之後） | ⚠️ 是 —— 新 validation gate 須先做影響統計 |

---

# L1 戰區七：特別行動組

| 🔴 | L2 優先級 | 任務 | L3 狀態 | L4 負責人 | L5 完成標準 | 預計工時 | 觸及紅線？ |
|---|---|---|---|---|---|---|---|
| | P2 本週 | 定「長題目上線順序」總表 —— 24 科唔可能同時開 | 📝 待資源確認 | **Yuna（COO）** | 上方學科軍團 5 行覆蓋 24 科，但真人審批係唯一樽頸（每科約 3 小時）。24 科 = 約 72 小時真人工時。完成 = 排出明確次序（建議按考生人數：數學 → 中文 → 英文 → 經濟 → 生物 → 化學 → 物理 → BAFS → 其餘），每週鎖定 1–2 科，唔好 24 科齊頭並進。 | 1h | ❌ 否 |
| | P3 本月 | 7 科未有指定學科首席 skill | 📝 待虛擬員工分配 | **Yuna（COO）** | 實測 188 個 skill 入面搵唔到：旅遊與款待、健康管理與社會關懷、音樂、體育、視覺藝術、科技與生活、倫理與宗教 嘅首席（共 7 科）。完成 = 每科指派兼任首席，或明文決定呢幾科只做 MC 唔做長題目。 | 1h | ❌ 否 |

---

## 🎯 建議執行次序（48 小時內）

依「最少人手、最大學生可見改善」排：

1. **OpenDyslexic 字型檔**（1h，純下載放檔）—— 唯一一項「一個檔案令一整個 SEN 功能由假變真」
2. **打氣牆 pending 帖**（0.25h）—— 學生等咗 4 日
3. **`WALL_SALT` 確認**（0.25h）—— 只有你查得到
4. **`math-long-batch-1` 簽名 + 上線**（真人 2–3h）—— 25 科入面第 2 科有真長題目做
5. **108 條孤兒課題**（3–4h）—— 已經供應緊學生但篩選唔到嘅題目
6. **`chinese-fanwen-weak-batch2` 簽名 + 上線**（真人 1.5h）
7. **ICT 偽代碼作答區**（3h）—— 解鎖 ICT 長題目嘅前置條件

第 1–3 項合共 **1.5 小時**，做完即刻有三樣嘢由「壞」變「好」。

---

## 📌 本清單冇包括嘅嘢（刻意）

| 唔列入嘅項目 | 原因 |
|---|---|
| `/dev/answer-cards`、`/dev/long-session` 生產環境 404 | **呢個係正確設計，唔係缺陷** —— 開發預覽面刻意 `notFound()`，唔應該上線 |
| 中文「曲二首／四塊玉／折桂令」零覆蓋 | **唔屬 DSE 十二篇指定文言範文** —— 十二篇為：論仁論孝論君子、魚我所欲也、逍遙遊、勸學、廉頗藺相如列傳、出師表、師說、始得西山宴遊記、岳陽樓記、六國論、唐詩三首、詞三首。實測 12 篇全部有覆蓋 |
| 任何收費／Premium／訂閱功能 | 憲章第 3 條永久禁止 |
| JUPAS 預測器、老師平台、gamification、假統計 | 憲章第 8 條已否決，復建 = 不合格 |
| 長答自動批改／關鍵字覆蓋率／Tone & Register 語義批改 | 已第 8 次復發嘅紅線，永久禁止 |
| 自動自殘／情緒 NLP 偵測 | SEN-07 已永久否決；責任版本 = 靜態熱線指引 |

---

## 🔍 本清單所有數字嘅實測出處

| 數字 | 來源 | 值 |
|---|---|---|
| 題庫總數 | `getSubjectQuestions()` 全 25 科加總 | 5,201（MC 5,175 + 書寫題 26） |
| 書寫題科目數 | `getWrittenQuestions()` | 1／25（只有中文） |
| 孤兒課題題目 | 逐科比對 `getSubjectTopics()` vs 題目 `topic` | 108 |
| 測試 | `npm test` | **204 pass / 0 fail** |
| Supabase 表 | `list_tables` (public) | 7 張：`profiles`(6)、`user_settings`(114)、`user_progress`(136)、`user_sessions`(**0**)、`review_decisions`(114)、`wall_posts`(1)、`wall_likes`(0) |
| 生產部署 | Vercel `list_deployments` | `8c7da8b` = HEAD，READY |
| 已安裝 skill | `ls .claude/skills/` | 188 |
| 概念網 | `docs/concept_network.json` v1.1 | 12 pieces / 4 node_types / 14 proven_combinations |
| 未簽草稿 | 逐個 `*.decisions.json` 讀 `_meta.reviewer` | 2 個未簽（20 條）、5 個冇 decisions 檔（27 條）、2 個死檔（7 條） |

---

*本清單由機器核實生成，所有「已完成／已審批」聲稱均經 Step A（filesystem）→ Step B（Supabase）→ Step C（live 題庫）三點核對。*
*任何入庫簽名位一律留白 —— 機器永不自動入庫。*
