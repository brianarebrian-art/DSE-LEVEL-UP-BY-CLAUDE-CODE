# 未來半年藍圖 v1.0 — 事實核查報告

- 核查日期：2026-08-13
- 來源：`~/Downloads/未來半年計劃.md`（46 個里程碑，6 個 Phase）
- 方法：filesystem + Supabase 實表（`aegekxapxgcfdrkzisis`）+ CHANGELOG
- 結論：**骨架可用，但有 1 項安全問題須即時處理、3 項已否決項目復建、1 項會反轉既有私隱決策。**

---

## 〇、本次核實到的實數（供全份文件校正用）

| 項目 | 藍圖聲稱 | 實測 |
|---|---|---|
| `profiles` | — | **6** |
| `user_progress` | — | **136** |
| `user_sessions` | — | **0 行** |
| `wall_posts` / `wall_likes` | 待 apply 0008 | **表已存在**，1 / 0 行 |
| `review_decisions` | — | 114 |
| public schema 表數 | — | **7**（`question_events` 已刪，未復建） |
| 現有 MAU | 「~200」 | **無法計算**（見 §4） |
| 已上線科目 | 「12 學科」 | **25 科** |
| 概念網節點 | 「29 個」 | 3 份檔、**18 節點**、11 類 |
| 每月成本上限 | 「$180.81 死鎖」 | `budget-guard.mjs`：2026-07-29 Brian 拍板改為 **US$200** |
| Build 路由數 | 「50 routes」 | 最近一次 build **83 頁** |

---

## 一、🔴 安全問題 — 須即時處理

### M5.3「我想傾下」→ 連接 Sarah(社工)

Phase 5 是考試期。設計是：學生考完試情緒低落 → 按「我想傾下」→ **接駁 Sarah（社工）**。

**Sarah 是虛構角色，不是真人。**

一個處於情緒危機的 12–18 歲學生，按下求助鍵之後接駁到一個不存在的社工，比沒有這個按鈕更危險 —— 按鍵本身構成了「有人接住你」的承諾，而該承諾無法兌現。

**替代做法（唯一負責任版本）：** 靜態求助熱線指引，明確標示是外部機構、不是平台員工。已核實號碼：

- 生命熱線 **2896 0000**
- 撒瑪利亞防止自殺會 **2382 0000**

（外部文件屢次寫成 2389 2222，該號碼有誤，不得沿用。）

同一原則適用於 M5.5「SEN功能使用率」等任何需要辨識學生心理狀態的指標：**不得對學生輸入做自動情緒／自殘偵測，不得儲存心理健康 PII。**

---

## 二、🔴 M1.1 C-Strict 會反轉一項已作出的私隱決策

藍圖要求「Zero-Local-Write：100% 業務數據嚴禁 localStorage，全部上雲」，並在〈跨階段核心守則〉列為**每次 commit 檢查**。

三個問題：

1. **C-Strict 是被否決的方案。** `CHANGELOG.md` 標題原文：「2026-08-05e — 結果服務端覆核（**C-Strict 唯一保留嘅一項**）」，同段並列出 C-Strict 的實際錯誤（例如 in-memory 限流在 serverless 下形同虛設）。全份 repo 只有 CHANGELOG 提及此名，沒有任何代碼採用。
2. **與現行架構相反。** CLAUDE.md §3 寫明「Data: 100% localStorage（客戶端）」，61 個檔案依此設計。照藍圖去掃「違規寫入 localStorage」，會掃出整個架構違規。
3. **最重要 —— 它會反轉一項刻意的私隱決策。** 同一份 CHANGELOG 解釋為何結果覆核採零儲存：

   > 「入面係『學生答錯咗啲乜』，對 12–18 歲平台嚟講唔存落 server 就冇得洩。」

   把全部業務數據上雲，等於把每個未成年學生的答題記錄、錯因診斷、情緒狀態集中儲存 —— 正是當初刻意避免的。

**建議：** 保留 localStorage 為主、server 只作備份的現狀。若要改，須 Brian + Yuna 雙簽，並先做 PDPO 影響評估。

---

## 三、🔴 已否決項目復建（憲章 §8）

| 里程碑 | 內容 | 憲章狀態 |
|---|---|---|
| **M3.2** | JUPAS 預測器 v1.0、入學機率、誤差 ≤10% | §8 **第一條**永久否決 |
| **M2.6** | 標題自寫「減壓緩衝區 **(Gamification)**」：房屋擴建、**完成題目解鎖**空間、虛擬貨幣購買家具、HHA 評分、社交展示 | §8 Gamification（解鎖機制）永久否決 |
| **M2.7** | 虛擬超市：虛擬貨幣、支付流程、**進度解鎖機制**、完成度評分 | 同上 |
| **M4.3 / M6.3** | 「**化城避風港**」、「動物森友會式」虛擬畢業典禮 | §8 舊名永久否決；2026-07-15 已改名「呼吸空間」 |
| **M4.6 Tier 3** | 「推出 **Premium 訂閱**（非盈利導向，僅覆蓋成本）」 | §3 嚴禁 |
| **M6.6** | 「潛在**收費模式**實驗」：進階報告、AI 溫習計劃、優先客服 | §3 嚴禁 |

M2.6 值得單獨一提：憲章允許的是「減壓緩衝區」放鬆功能（呼吸動畫、金句、休息按鈕），**明文要求與 gamification 劃清界線**。藍圖直接在標題把兩者劃上等號。

M4.6 Tier 3 與 M6.6 的「僅覆蓋成本」「不強求收入」措辭不改變性質 —— 一旦有付費層，平台就不再是 100% 免費。

---

## 四、🟠 目標無法測量（`user_sessions` = 0 行）

藍圖以 MAU 為主要增長指標：~200 → 500 → 1,000 → 3,000 → 5,000 → 10,000。

**`user_sessions` 表 0 行 —— 前端從來沒有寫入過。** 因此：

- MAU、7 日留存、30 日留存、平均每日做題數（M5.5 全部五項）**現時無法計算**
- 「現在 ~200」這個起點數字沒有來源；可查的實數是 `user_progress` 136 行

同類問題：

- **M1.7 / M4.4 等級預測誤差 ≤0.5 Grade、交叉驗證準確率 ≥90%** —— 需要學生真實 DSE 成績作對照。藍圖自己在 M5.4（2027 年 4–5 月）才開始收集，卻要求 M1.7 在 **2026 年 9 月底**驗收。時序不成立。
- **M6.1「用戶升分數據：使用前後模擬試分數對比」** —— 沒有前測基線。
- **M4.5 模擬 10,000 同時在線壓力測試** —— 對 136 個註冊用戶的平台做萬人壓測，且 Vercel Hobby 壓測本身就會爆額，與成本上限直接衝突。

**建議：** 若要用 MAU 作指標，Phase 1 第一件事應該是**令 `user_sessions` 真的有寫入**，否則其後五個 Phase 的所有增長目標都無從驗收。

---

## 五、🔴 長答自動批改 —— 第 16 次

| 里程碑 | 內容 |
|---|---|
| **M2.1** | 英文 Paper 2 Tone & Register 雙重語義批改引擎，驗收「批改報告須明確指出『此處語氣過於口語，應改為 formal』」 |
| **M2.2** | Paper 3 盲抄檢測，準確率 ≥85%、誤報率 ≤5% |

`data/questions/types.ts` 載有硬性規則：**書寫題永不由機器批改**，亦不計入客觀準確率與等級預測（2026-07-31 Brian 拍板）。此紅線至今已在 16 份外部文件出現。

M2.2 的「準確率 ≥85%、誤報率 ≤5%」另有一層問題：無標註資料集即無從量度，該數字無法驗收。

---

## 六、🟠 其他需要修正之處

| 里程碑 | 問題 |
|---|---|
| **M1.3** | 「29 個概念節點」→ 實際 18 個、3 篇（十二篇中的 3 篇）。目標可以定 29，但不能寫成現況 |
| **M3.4** | 「顯示同時在線人數」—— 136 個註冊用戶，此數字長期是 0–1；先前已否決製造 ±2 的假在線數 |
| **M4.2** | 「同樣錯呢題嘅人有幾多」需要 `question_events` 表，該表 2026-07-14 已刪且未復建 |
| **M3.7** | 連登「**無意中分享**」策略 = 假扮普通用戶推廣（astroturfing）。與「戰友式、誠實」品牌定位互相矛盾 |
| **M3.8** | 邀請信點名 **Susie、Charlene** 等真實補習老師，未經同意寫入內部文件；信中「免費 **AI** 溫習平台」—— 平台沒有 AI 元件 |
| **M1.6** | 向學生輸出「你重複犯同一類錯，**代表你無真正理解概念**」—— 語氣屬指責，違反 §7 大愛設計。改為「呢個課題值得再行一次」之類 |
| **M6.5** | 「新增科目評估：中史、歷史、地理、公社科」—— 這些科目**已經上線**（現有 25 科） |
| **守則** | 「Build 永遠 green（50 routes）」→ 實際 83 頁；「成本 $180.81」→ budget-guard 已改 US$200。兩處數字須與代碼對齊 |

---

## 七、✅ 可以照做的部分

以下里程碑不觸紅線，且已有工具支援：

| 里程碑 | 現狀 |
|---|---|
| M2.3 經濟科題庫擴展 | `term-guard.mjs` 已攔共用品／企業家職能／超綱彈性 |
| M2.5 理化生題目上線 | 管線齊備：`_gate.mjs` → `review-drafts.mjs` → 真人批 → `promote-drafts.mjs` |
| M3.5 中文跨篇比較擴展 | `arts-variant-factory.mjs` 已可由概念網展開草稿 |
| M1.4 數學參數化擴展 | 引擎在 `_parametric.ts`；2026-08-13 已加損耗登記冊，可見各科丟棄率 |
| M6.4 考綱變更追蹤 | `hkeaa-monitor.mjs` 已有（註：HKEAA 封鎖程式抓取，只能做人手覆核提醒） |
| M6.2 用戶見證 | 文件自己寫明「禁止虛構統計/見證/人數」—— 此條正確，應保留 |
| M1.5 SEN 優化 | 見下 |

### M1.5 有一項一步可完成

`public/fonts/` 目前只有 README，**真・OpenDyslexic 字型檔未安裝**。現時靠 CSS fallback（Verdana／Tahoma 等 BDA 建議堆疊 + 加寬字距），功能可用且已在 README 明文說明，屬刻意設計而非故障 —— 但真字型始終未上。

官方 https://opendyslexic.org 採 SIL Open Font License（免費、可商用）。下載 Regular、轉 `woff2`、命名 `OpenDyslexic-Regular.woff2` 放入 `public/fonts/` 即自動生效，$0、零 CSP 例外。

---

## 八、其他須創辦人處理的事項

1. **`WALL_SALT`** —— `lib/wall/identity.ts` 的 fallback 鏈最終落在硬編碼字串 `'shadow-study-room'`。若 Vercel 未設此環境變數，打氣牆的匿名化即失效。**只有創辦人能查**（本機無法讀取 Vercel 環境變數）。產生方法：`openssl rand -base64 33`，**直接貼入 Vercel，不要貼入對話、不要寫入檔案**。
2. **`WALL_ENABLED` 缺省為 `true`** —— 打氣牆現時預設開啟，`wall_posts` 有 1 篇待審。人手審核流程須先就位。
3. **成本上限文件打架** —— CLAUDE.md 憲章 §5 寫 HK$180.81，`budget-guard.mjs` 寫 US$200（2026-07-29 Brian 拍板）。兩份都是現行文件，須定其一。

---

*本報告由 Claude 核查產出，未經 Brian／Yuna 簽署，不構成任何項目批准。*
*§一 至 §三 屬建議「不予執行」；§四 至 §六 屬「修正後可執行」；§七 可即時開工。*

---

# 附錄 A — v1.1（AUDIT 修正版）覆核

- 覆核日期：2026-08-13
- 對象：`~/Downloads/未來半年計劃 (1).md`
- 結論：**大部分修正到位；4 項原問題未修，2 項為 v1.1 新引入，3 處版本內自相矛盾。**

## A1. 已確認修好

M5.3（真實外部熱線 + 明示非平台員工 + 號碼正確）、M3.2 刪除、M4.6／M6.6 刪除、
M4.3／M6.3 改名「呼吸空間」、M3.7 改坦誠分享、M3.8 刪具名 KOL 及「AI」字眼、
25 科／83 頁／US$200、M1.7 時序、新增 M1.8（`user_sessions` 寫入）、
附錄加註「Sarah 為虛構角色」。

## A2. 🔴 v1.1 新引入的問題

### A2.1 M1.1 挪用 `WALL_SALT` 去哈希行為數據

驗收標準寫「匿名化行為數據經 WALL_SALT 哈希」。實查 `lib/wall/identity.ts`：

- 該 salt 為 `server-only`，全站**僅一處**使用（`app/api/wall/route.ts`）
- 其設計是**每日輪換**（HKT 04:00 日界線），目的正是令同一用戶**跨日不可追蹤**

兩個問題：

1. **共用推導秘密會令兩個系統可以互相關聯** —— 打氣牆的匿名發帖與行為數據會共享同一條推導鏈，本來互不相干的兩份假名資料因此可以 join 回同一個人。
2. **需求方向相反** —— 打氣牆刻意要「跨日不可追蹤」；行為分析恰恰需要跨日連續性。同一個 salt 無法同時滿足，必有一方失效。

**建議：** 行為數據若要匿名化，用獨立 salt 及獨立輪換策略，不得沿用 `WALL_SALT`。

### A2.2 M5.5 新增指標指定使用已刪除的表

新表格「平均每日做題數 | 數據源：**`question_events` 表**」。

該表於 2026-07-14 隨教師雷達一併刪除，本次覆核 public schema 仍為 7 張表，**無此表**。v1.0 只是在 M4.2 隱含需要它；v1.1 反而明文把它寫成數據源。

## A3. 🟠 原問題未修

| 項目 | 狀態 |
|---|---|
| **M1.6** | 驗收標準逐字不變：「你重複犯同一類錯，**代表你無真正理解概念**」。語氣屬指責，違反 §7 大愛設計 |
| **M4.2** | 「同樣錯呢題嘅人有幾多」仍需 `question_events`（已刪） |
| **M3.4** | 「顯示同時在線人數」仍在。136 個註冊用戶，此數長期 0–1；製造假數已先前否決 |
| **M6.1** | 「使用前後模擬試分數對比」仍無前測基線 |
| **M4.5** | 由「10,000 同時在線」改為「高並發」，只改措辭：Hobby Tier 壓測本身即會爆額，與 US$200 上限衝突未解 |

## A4. 🟠 同一版本內自相矛盾

1. **M2.6 刪除 Gamification，但〈跨階段核心守則〉→ 用戶體驗仍寫「成就系統：虛擬徽章 + 進度條 + 多巴胺爆發點」** —— 逐字保留，未隨 M2.6 一併移除。
2. **M5.4 仍有「解鎖『DSE 2027 畢業生』虛擬徽章」** —— 徽章／解鎖屬同一類機制。
3. **M1.2 要求「apply `0008_wall_posts.sql`」，但 M1.1 驗收標準自己寫「現有 1 篇 wall_posts 待審」** —— 表已存在且有資料，migration 早已 applied。

## A5. 修正方向正確但現時無法落地

**M2.1／M2.2**（英文卷二／卷三結構化自評指引）方向完全正確 —— 已改為學生自評，無機器打分。但實測：

```
english: 共 149 條 → 全部 mc（書寫題 0 條）
chinese: 共 193 條 → mc 167、long 26
```

**英文科現時沒有任何書寫題**，亦即沒有可供自評的提交介面。要落地，須先有英文書寫題入庫（走 drafts → 真人逐題批 → decisions.json 管線），自評清單才有依附對象。中文科有 26 條 `long`，可先在中文科試行。

## A6. 增長指標仍需再調

M2.8／M3.9／M4.8／M6.7 改用 `user_progress` 為基準。此欄為**累積**進度列數，只升不跌，不反映活躍度 —— 以它作增長目標，等於「開過一次即永久計數」。

既然 v1.1 已新增 M1.8（Phase 1 實現 `user_sessions` 寫入），建議 **Phase 2 起改回 MAU**，`user_progress` 只作 Phase 1 的過渡基準。

---

*附錄 A 由 Claude 覆核產出，未經 Brian／Yuna 簽署。*
