# 創辦人決定紀錄 —— 2026-09-26（第二批）

> **決定人：** Yuna（COO），2026-09-26，按憲章 §18 即時生效，毋須簽名。
> **來源：** Yuna 在 session 中貼上的一份決定清單（「我按新 §18 直接決定晒」）。
> 本文件逐項記錄：決定的內容、實際做了甚麼（commit），以及未做的原因。
> 未做的項目不是否決，而是決定所依據的描述與 repo 實況不符，需要 Yuna 按實況再決定一次。

分支：`release/2026-09-26-b`（由 `main` 的 `67eba34` 開出，未推送）。

---

## 已執行

| 項目 | 決定摘要 | 實際做法 | commit |
|---|---|---|---|
| A2 兩張分享卡並存 | 做；預設新卡，舊卡稱「傳統成績卡」 | 結果頁可揀「錯因破解卡」（預設）或「傳統成績卡」。錯因卡只列今日在該科自診的錯因及一句提醒，不顯示正確率、次數、用時、難度分佈；用莫蘭迪淺色；不印 IG 連結。今日沒有錯因紀錄時，錯因卡選項變灰，改用成績卡（影響報告方案甲） | `33596cb` |
| F1 按錯因揀題 | 做；「重複模式」卡加「即刻練返呢類」 | 「你搵到一個規律」卡加連結，開一節 10 題 `mode=cause`。只重排題目次序，3:5:2 不變。審題陷阱用指令字詞庫（中英題幹），概念盲區／運算粗心用 `dse_reverse_log` 記錄過的課題。瀏覽器實測：10 題全部命中，比例 3:5:2 | `e386048` |
| 選修選擇上雲 | 做 | 按 `docs/charter-amendment-2026-09-26-electives-DRAFT.md`：`dse_electives` 經 `lib/sync.ts` 進度通道上雲，逐科比較，較新的選擇勝出。私隱頁（`POLICY_VERSION` 2026-09-26.v2）、`StoredDataInspector`、上雲白名單測試及憲章 §16.E 同日更新 | `ebfa906` |
| B5 安靜模式 | 先驗證，通過就做 | 驗證結果：儀表板四格數字卡（含正確率）排在安靜模式開關之前，而且安靜模式收不到，外部報告屬實。已改：開關移到數字之前；安靜模式收起分數行、數字卡、練習頁總時間、答錯難題後自動彈出的情緒溫度計、結果頁打氣牆；「每題計時」掣保留 | `34fcbe0`、`020adc1` |
| 「書寫卷未涵蓋」文案 | 刪 | 首頁及科目頁兩處刪去。實況：25 科均有書寫題 | `34fcbe0` |
| 指引未見的 220 題 | 收起，不向學生展示，保留作審計 | `data/questions/hidden-topics.ts` 收起倫理與宗教 `religion_philosophy`、`religion_society`。題目檔不刪。全站 27,326 → 27,106 題。紀錄及放回步驟見 `docs/UNMAPPED-220.md` | `e29b8c5` |
| orchestrator skill | 「需簽名」改為「已表達立場即決定（§18）」 | `.claude/skills/dse-level-up-orchestrator/SKILL.md` 紅線 1、2 及核查協議改寫；同時刪去「gamification 屬已否決項目」（2026-08-22 已解禁，§8.1）。該目錄不在 git 內 | —— |
| 部署日期 | 填寫 | `docs/difficulty-family-unification-2026-09-26.md`：production 部署 2026-09-26 已核實；雲端題庫同步未完成 | `34fcbe0` |

### 瀏覽器實測（localhost:3001，2026-09-26）

B1 休息日全頁、B3 情緒溫度計（焦點在標題，按兩次 Enter 不記錄亦不跳題）、B5、F1、A2 均已實測。
A1 以 curl 核實 production 已提供新段落。C1（離線）及 D1（要登入）未實測。

### Production build

`npm run build` 通過，112 個靜態頁面。

---

## 未執行 —— 須按實況再決定

### 1. `/cantonese` 範圍（c）

**決定原文的描述：** `/cantonese` 為獨立入口，只包含已有粵語語音／解釋的題目，範圍鎖定 220 題以外有粵語欄位的題。

**實況：** `/cantonese` 不是題目，而是 16 個生活場景、96 句及 128 個詞語的粵語學習頁；與「220 題」無關。
`docs/charter-amendment-2026-09-19-DRAFT.md` 的選項 (c) 是「納入，但擺出 DSE 產品之外，另起一個地方做」，
即搬到另一個網站。這需要決定放在哪裏、由誰承擔成本（§5），Claude 不能代為開設新網站或新 Vercel 項目。

**需要決定：** (c) 是否指搬到另一個網站？若是，放在哪裏？若只是想把入口從主導航移走、頁面保留，請直接說明。

### 2. 取消實名審批後的新題把關

**決定原文：** 「AI 初審 + 創辦人覆核，唔再實名」；新題入 pending 池，`lib/qbank/qualityGate.ts` 檢查通過後**自動入 approved**，儀表板有「待覆核」掣可一鍵退回。

**實況：**
- 「自動入 approved」與憲章 §2、§12「**機器永不自動入庫**」直接衝突；「創辦人覆核」與「自動入庫」在同一項內亦互相矛盾（覆核在上線前還是上線後？）。
- `lib/qbank/qualityGate.ts` 不存在；現有自動檢查是 `review-drafts.mjs`。
- 儀表板是學生頁面，不宜放審題掣。

**需要決定（二選一）：**
- (甲) 上線前覆核：AI 檢查只作篩選，創辦人一鍵批准後才入庫，紀錄只寫「創辦人」及日期，不寫實名。§2、§12 不變。
- (乙) 上線後覆核：通過檢查即自動上線，創辦人事後退回。須同時修改 §2、§12。

### 3. 家族統計草案

**決定原文：** 不接受，因「家族統計會暴露屋企人學習數據」；改做「同你同級嘅 100 人平均」匿名對比，雙方 opt-in。

**實況：**
- `docs/charter-amendment-2026-09-26-family-stats-DRAFT.md` 的「家族」是**題目模板家族**（同一條參數化題目的不同數字版本），與家人無關。
  草案收集的是每個題目家族的**匿名**作答及答對次數，只用來重新推算難度標籤：不記錄誰作答、不記錄選了甚麼，
  亦不向任何學生顯示或作學生之間的比較。
- 替代方案「同級 100 人平均」屬跨用戶比較，憲章 §16.E 約束 3 明文禁止（「不得據此製作……同儕對比、百分位排名」），執行第 3 點亦禁止提出較窄的替代方案繞過該約束。

**需要決定：** 按草案原意（題目模板家族的難度統計）重新考慮是否接受。

### 4. 選修篩選擴大

**決定原文：** 擴大為「主科 + 選修單元 + 錯因」，儀表板篩選器加兩項，預設摺埋。

**實況：** 原問題是 `lib/electives.ts` 的 `TOPIC_SCOPE` 要不要加入目前刻意不對應的課題
（地理 `weather_climate`、ICT `databases`、企財／設計與應用科技／健康管理與社會關懷的混合課題）。
這些課題同時包含必修及選修內容，擴大前要逐題分開，否則會把必修題篩走。儀表板目前沒有篩選器。

**需要決定：** 是指擴大 `TOPIC_SCOPE`（要逐題分課題），還是新增儀表板篩選器（新功能，要先出影響報告）？

### 5. Vercel 離線功能

**決定：** 不開。**無需改動** —— 目前 Vercel 沒有設定 `NEXT_PUBLIC_SW_OFFLINE`，`.env.example` 留空，
留空即 production 關閉。若把 `.env.example` 改為 `0`，只會令本機開發也關閉，production 結果相同，故維持留空。

### 6. 憲章 §8.1 與 gamification

**決定原文：** §8.1 原文「零上癮設計」與打卡、連勝矛盾；改為允許正向連勝，設休息日中斷保護。

**實況：**
- §8.1 沒有「零上癮設計」一句；§8.1 已經准許連擊、EXP、段位等。
- 每日連勝（「連續 N 日」）是 2026-08-23 創辦人以 HOTFIX-0823 **刻意移除**的，改為累積日數（`lib/logicLog.ts`、`lib/progress.ts`），並有 no-daily-streak 測試鎖住。理由是「中斷一日即歸零」對學生是壓力來源（§7）。

**需要決定：** 是否要恢復每日連勝（推翻 HOTFIX-0823），並在休息日不中斷？若是，會改代碼及 §8.1。

---

## 仍待 Yuna 本人執行

1. 用 GitHub Desktop 推送 `release/2026-09-26-b`，合併入 `main`。
2. Vercel 部署完成後，執行一次：`npx tsx scripts/qbank/sync-questions.mts --push`
   （今日首次執行中途斷線，25 科只同步 4 科；Claude 重試被自動模式攔截）。
   先部署後同步：倫理與宗教的題庫版本在本分支已改變，同步必須在新版本上線後進行。
3. 同步完成後，在 `docs/difficulty-family-unification-2026-09-26.md` 填寫同步完成日期。
