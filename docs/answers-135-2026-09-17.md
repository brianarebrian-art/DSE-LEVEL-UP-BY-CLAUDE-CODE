# 45 人 × 3 條 = 135 條問題 —— 逐條回答（2026-09-17）

**每個數字都係今日喺 `morandi-push-v4.0` 實跑，唔係抄會議紀錄。**
指令列喺文末，可以自己重跑。憲章 §16.C：冇跑過嘅一律唔填。

今日實跑結果：

| 閘 | 結果 |
|---|---|
| `npm test` | **953 / 953**，0 fail |
| `npm run qa` | **exit 0**（10 閘全綠） |
| `npm run sync:conflict-repro` | **4 / 4 綠**，情境 4 = `math::algebra=36/9` |
| `scan-en-cjk` | 745 真債 / 122 中文科內 / 18,626 單語設計 |
| `check-posref --banks` | 208 檔，293 條祖父清單，**零新增** |
| `qbank:charscan --all` | 6 處 / 2 檔，零新增 |
| 草稿待批 | 145 條 / 20 檔（`*-written-b4` 140 條 / 17 檔） |

---

## §0 問題入面十個錯前提（先更正，唔更正就會照錯嘅前提去改嘢）

| # | 問題度寫 | 實測 | 指令 |
|---|---|---|---|
| 1 | 「dev = `next dev -p 3001` 無 `--webpack`」（6.1） | **有**：`next dev -p 3001 --webpack`（2026-09-15 補返，同日 CI 加咗閘） | `node -p "require('./package.json').scripts.dev"` |
| 2 | 「`text-muted-foreground` 誤用要改」（9.1、27.3） | **0 處**，已經清晒 | `grep -rn text-muted-foreground app components \| wc -l` |
| 3 | 「`charter-review-dates` 測試紅」（36.1） | **綠**，953/953 冇一條紅 | `npm test` |
| 4 | 「數學 12 課題教案 **48 份**」（20.3、30.3） | **12 張卡**，`batch2-uncovered` | `data/sensei/math/drafts/batch2-uncovered.decisions.json` |
| 5 | 「**751** 英混中」（5.1、27.1） | **745**（六科，中文科以外） | `npx tsx scripts/qbank/scan-en-cjk.mts` |
| 6 | 「題庫 **98.9%** 有 3 條+」（3.3、32.3） | **100%**：298 個課題全部 ≥3 條 | 見文末 ⑧ |
| 7 | 「三個 commit 推唔到」「ccf52c5 未 push」（7.1、14.2） | **四個全部已喺 origin**，branch 0 ahead | `git status -sb` |
| 8 | 「AnonymousPulse／`pulseHelpful`／`dse_pulse_last_shown`」（12.2、40.3…） | **一個字都未寫過** —— 係提案，唔係代碼 | `grep -rn "AnonymousPulse\|dse_pulse" .` → 零命中 |
| 9 | 「`user_sessions` 加 `ab_group`」（12.1、33.1） | `ab_group` **唔存在**；`user_sessions` 得 `user_id` ＋ `day` 兩欄 | `app/api/sync/session/route.ts:50` |
| 10 | 「manifest 仲有 `#00726C` 殘留」（5.2、41.2） | manifest 已係 `#F4F0EA`；但 **`#00726C` 仍然係活嘅 token**（`--color-accent-strong`，111 處用緊） | `app/globals.css:68` |
| 11 | 「CI 點樣先捉到 middleware.ts／冇 `--webpack`」（4.2、6.3） | **已經捉緊**：`ci.yml` 有 `Build config guard` 一步，兩樣都 fail-fast | `sed -n '20,30p' .github/workflows/ci.yml` |

⚠️ 另外：**「同構標記」（`archetype` / `isomorphic`）全 repo 零命中。**
所有講「archetype 欄 optional 定 required」嘅問題，講緊一樣**未存在**嘅嘢。

---

## 1. Brian Ho CEO

1. **今日係第 8 日，唔係第 7 日**（09-09 → 09-17）。三條路，成本唔同：**即日雙簽** = 零代碼改動；**復原** = `SESSION_SIZE` 改返 20 ＋ `git revert` 反思鎖 ＋ 拆走 10-09／11-09 兩個覆檢日測試，而且 10-09 覆檢會冇數據；**明確唔簽但准許實驗** = 憲章要寫低「實驗中，未生效」。我唔可以替你揀，但第三條路至少要有人寫落去，而家係「冇人答得到」嗰種空窗。
2. 「聽日返唔返嚟」：`npm run analytics:retention` 已經計得到 Day-2，但**只量到做過題嘅人**（`user_sessions` 2026-09-11 先重建，之前 0 行）。「做完覺唔覺得啱」：**冇任何嘢量緊**，要先簽 `962e2bd`。
3. 唔會被洗走 —— `ccf52c5` 喺 origin，檔案喺 repo，任何 new chat 讀 `docs/signing-status-2026-09-16.md` 就見到。真風險係**數字漂移**：表入面每格都附咗重跑指令，讀嘅人要重跑先當數。

## 2. CVO Vision

1. 建議改憲章跟 `package.json`（草案 `87cdeaa` §一已經寫好條文），另加一句「降版要寫理由」。⚠️ `ci.yml` 已經有 `Build config guard`（守 `--webpack` 同 `middleware.ts`），但**冇守版本號** —— 真係要 lock 就喺同一步加一行。
2. 三層寫法（唔自相矛盾）：預設淺色莫蘭迪 `#FAFAF8`／`#1A1A1A`／`#006B65` → 暗色 `cyber` `#1a1917` ＋ sage → **霓虹四色只限導出 PNG 圖卡**（`DailyStatsCard.tsx:33`、`ResultPageClient.tsx:192-193`）。`#57685C` 係 `--color-subj-sage`／`--color-ml-sage`，唔係主 accent。
3. **冇 runtime 監測**（product 內零 error-reporting）。所以「零故障保證」證明唔到，撞正 §16.D。建議刪，改成「每項都有迴歸測試鎖住，改壞咗會喺 CI 紅」。

## 3. CDO Detail

1. 而家守零新增嘅係 CI（`ci.yml` 跑 `npm run qa`，內含 `check-posref --banks`）。要 pre-commit：**唔可以裝 husky**（§5 嚴禁新增套件），寫 `.git/hooks/pre-commit` 純 shell 就得（零依賴）。
2. `check-posref.mjs:81` 只掃 `explanation`／`explanationEn` 兩個欄，唔做全文 regex —— 正正因為數學嘅「第三項」係數列的項。檔頭 35-36 行已列低要排除嘅：混淆變項、雙重認證、多項式的項。
3. **optional。** required 會即刻令 9,183 條參數化題全部 fail —— 憲章 §6 明文：加閘之前先數影響，唔准以功能改動令現有數據集失效。

## 4. Kaizen

1. 因為 stale CSS 根因喺 `.next/dev/cache`（380MB），唔喺 `.next/cache/webpack`（690MB）。刪成個 `.next` 要全站重 build，慢好多；`npm run clean` 仍然保留畀真係要核彈嘅時候。
2. **已經有。** `ci.yml` 嘅 `Build config guard` 一步同時守兩樣：`package.json` 嘅 `build` 同 `dev` 都要有 `--webpack`（冇就 `process.exit(1)`），`middleware.ts`／`src/middleware.ts` 存在就 fail。而且佢**擺喺 `npm test` 之前**，錯誤訊息講明原因，唔使等 build 跑幾分鐘。
3. README 要寫嘅係根因，唔係指令：「見到改咗 CSS 冇效，跑 `npm run dev:fresh`；`rm -rf .next` 唔係錯，但白做多 690MB。」要我加我就加。

## 5. Brand Guardian

1. 次序問題要你哋定。但有一個硬約束：badge 如果對住 745 處未清嘅題顯示「機檢通過」，就係一個**兌現唔到嘅對外標示**（同 §16.D 同一種病）。所以要麼先清債，要麼 badge 加一個「英文欄未清」狀態。
2. **冇殘留**：`app/manifest.ts` `theme_color`／`background_color` 都係 `#F4F0EA`，第 15 行嗰個 `#00726C` 只係註釋記低舊值。`owl.svg` 六隻色：`#2C2A29 #3D3A38 #57685C #706347 #F0EBE3 #F4F0EA`。
3. 唔算雙重標準，但要講得出理由：導出 PNG 會**離開網站**（學生 share 畀人），唔跟用戶 A11y 變數走，所以鎖定標準色版。呢句要寫入 §14，唔寫就下個 session 會當佢係漏網。

## 6. Ken CTO

1. **前提錯**：`dev` 有 `--webpack`（實測 `next dev -p 3001 --webpack`）。憲章 §3 嗰句「Build: `--webpack`，dev port 3001」冇寫錯。
2. 我冇網，查唔到 upstream changelog，所以唔會答「冇 break change」。**我實測到嘅係**：16.3.2 之下 production build 93 頁綠、離線四情境（A/B/C/D）綠。要確認 upstream 要有人上網睇。
3. **已經自動 block 緊。** `ci.yml` 嘅 `Build config guard` 見到 `middleware.ts` 或 `src/middleware.ts` 就 `exit 1`，訊息叫人睇 `proxy.ts` 檔頭。理由寫喺 workflow 註釋：Next 16 見到佢同 `proxy.ts` 並存會喺 build throw。

## 7. Zack DevOps

1. `git credential.helper = osxkeychain`，我個 shell 攞唔到 keychain；`gh` 未裝。**而家四個 commit 全部已經喺 origin**（你自己推咗），branch 0 ahead。
2. `morandi-push-v4.0` 唔使 rebase —— 開 PR 就得。要 rebase 嘅係 `main`（本地 `main` = merge commit `3e4ba13`，唔係 `origin/main` 嘅後裔）。
3. CI 每次 fresh checkout，根本冇 `.next/dev/cache`。呢個純粹係本機問題，唔影響 CI cache hit。

## 8. Yuki Backend

1. **分唔到。** `recordAttempt()` 得三個呼叫點（`PracticeSession.tsx:562`、`:601`、`AnswerSheetClient.tsx:230`），全部喺交卷之後；`LongPracticeSession` 根本唔寫。「做咗 3 題走人」同「冇開過」喺數據上**一模一樣**。呢個就係 `962e2bd` 要補嘅窿。
2. 純 `localStorage` 計數 = **零 Edge request**。一上雲就係新 key，§16.E 執行第 1 點要創辦人書面批准。
3. 問題本身有誤解：`dse_progress` 寫 `localStorage`，唔係每題打 Supabase；同步係 debounce 推**整個 snapshot**。真正風險係 `lib/progress.ts:112` 嗰個 `slice(-500)` —— 長期用家舊紀錄會靜靜哋被截走。

## 9. Leo Frontend A11y

1. **已經 0 處**，唔使掃。
2. 因為 `html.focus-light` 前綴保證「熄咗燈就乜都唔變」。冇前綴嘅話，關咗 Focus 燈嘅學生一樣會見到 hover 改 opacity —— 一個關咗嘅功能唔應該仲有殘影。
3. **唔會閃爍**：`globals.css:760-766` 係一次 `opacity 0.25 → 1`，200ms，唔係循環動畫，遠低於 3Hz；而且 `html.no-motion` 通用選擇器已經覆蓋咗呢個 transition（唔使另寫）。⚠️ 但呢句只可以講「我哋自己嘅動畫低於 3Hz 而且有測試鎖住」，唔可以講「攔截」（§16.D）。

## 10. Ivy QA

1. 而家 `scan-en-cjk` **唔喺** `qa` 閘入面。要入閘得先定基線（745 係債定係現狀），否則加咗即刻紅。技術上加個 `--check` 模式對住基線數就得。
2. 已經喺 `qa` 入面：`check-posref --banks` 係第二閘，208 檔 293 條祖父清單，零新增。唔使再加去 `validate-banks`。
3. 已經逐個列咗：`npm run qbank:charscan -- --all` 印出 6 行，每行帶檔名 ＋ 題 id ＋ 欄位（見 `docs/content-debt-2026-09-16.md` §C）。

## 11. Milo Performance

1. 因為 build 嗰陣 `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:9` 打唔通，題目一定行靜態 chunk 回落路徑（§3.1 約束 4）。實測 B 情境：斷網 reload 照出返第 1／10 題。
2. anon key 留住冇 CORS 誤報 —— 打 `127.0.0.1:9` 係連線層面失敗，連 preflight 都去唔到。但 console 會見到 `Failed to fetch`，呢個係預期，唔應該當 error 彈（見 13.2）。
3. `sw.js?offline=1` 變返 `sw.js`，新 SW 喺 `activate` 會刪走所有 `dse-offline-*` cache（`public/sw.js:163` `CACHE_PREFIX`）。實測 D 情境綠。

## 12. Anya Security

1. `ab_group` **未存在**。加咗算唔算新 PII：`user_sessions` 而家得 `user_id` ＋ `day`，加一個分組欄唔係新身分資料，但**係新一欄綁住 `user_id` 嘅行為資料** —— 照 §16.E 執行第 1 點要書面批准。私隱條款 `2026-09-11.v2`（`lib/privacy/consent.ts:34`）冇涵蓋。
2. Pulse 未存在。真要做：上雲就係第四個 key，§16.E 約束 6 寫明「第四個 key 一樣要重新開題、重新簽名」。
3. 「全港 N 人」踩兩條：§8 禁虛構統計、§16.E 約束 3 禁跨用戶匯總比較。「有同學同你一齊溫」冇數字、冇比較、冇排名，所以唔踩 —— **但前提係唔可以喺後台真係做緊跨用戶聚合再講呢句**。

## 13. Ryo Mobile

1. iOS 仲要測三樣：加到主畫面之後 `apple-icon`（180×180，已生成）出唔出到、standalone 模式 safe-area 有冇被 notch 食、iOS Safari 對 SW cache 嘅 7 日清理。**呢三樣我冇測過，唔填。**
2. 探測失敗要 `catch` 咗當「雲端唔可用」走回落路徑，唔可以 `console.error` —— 學生開 DevTools 見到紅字會以為個站壞咗。
3. **已經有** `owl-maskable-512.png`（purpose: maskable），Android 圓形／水滴遮罩靠佢。iOS 唔用 maskable，用 `apple-icon`。

## 14. Finn Release

1. 三個工程債 commit 已經喺 `origin/main`。`morandi-push-v4.0` 開 PR 就得，merge commit 唔阻礙開 PR。
2. **`ccf52c5` 已經 push 咗**（branch 0 ahead）。三份草案保持分開 —— 佢哋等三批唔同人簽，squash 咗就冇得逐份 revert。
3. 出 amendment（`docs/charter-amendment-2026-09-17.md`），**跟住**同日改 `charter.md`。§16.E 記低過：批准同落實係兩件事，分兩日做就會漂移。

## 15. Sage AI Safety

1. 分界線唔喺語氣，喺**兌現唔到嘅承諾**。`claims-guard.mjs:51` 掃嘅係「保證升／包升／必勝／一定入到」。「搶分」係口號唔係承諾，過到閘；「致命錯因」唔係承諾但撞 §7 大愛紅線（打擊自信）—— 建議用「你發現咗一個新盲點」。「做 1 題」係最細嘅行動邀請，冇承諾成分。
2. 因為佢係一個**會改變學生安全行為**嘅假聲稱：一個靠螢幕閱讀器／光敏防護嘅學生，見到「零故障保證」可能唔開自己部機嘅輔助功能。同 §16.D「自動攔截 3Hz」嗰宗判例一模一樣。
3. `PracticeSession.tsx:689`：Enter **只喺焦點唔喺任何掣上面**先接手。所以 Tab 到「下一題」撳 Enter 照樣係撳掣，螢幕閱讀器用家嘅行為零改變。

## 16. Chloe UX Writer

1. 睇中文係咪**句子成分**。「utilitarianism it sits in 行為實際造成的整體後果」—— 冇咗中文句子就崩，係漏譯。「Largo (廣板)」中文喺括號入面，剷走英文照通，係對照。745 處入面倫理宗教 547 ＋ 音樂 100 屬前者。
2. 三粒掣（有呀／仲好／未係）係**唔可以聚合成平均分**嘅刻度；星星係分數，一有分數就會有人想計平均、排名、比較 —— 直接行返 §16.E 約束 3 條紅線。
3. 因為連續打卡本身就係 §7 禁嘅嘢（`no-daily-streak.test.mts` 守住）。休息日護盾更加唔可以顯示「你斷咗」—— `lib/restDay.ts` 檔頭寫明：❌ 真鎖、✅ 摩擦，一個真鎖會令唔想溫嗰個覺得「破咗戒」。

## 17. Ada PM

1. 兩個日子對應表入面兩行：10-09 覆檢 §7.1（`SESSION_SIZE`，要中途離開數據 → 靠 `962e2bd`）；11-09 覆檢 §7.2（要逐週正確率 curve → 靠 `analytics:retention`）。兩條路都**未有數據**，簽名表第二欄就係講緊呢件事。
2. 一份檔案一個決議。而家兩次投票（32:13、29:16）結論唔同但方向唔衝突 —— 內容債先、憲章排下一項。寫入 `docs/` 就唔會重複投。
3. `economics-floor.json`（`brian`）同 `ths-floor2.json`（`Brian`）各剩 1 條。`_reviewer-gate.mjs` 會擋代簽，所以 PM 只可以追人，追唔到就係卡住 —— 呢個係設計，唔係 bug。

## 18. Dr.Chan 中文

1. 122 處要你判。**預設唔當債** —— 範文原句、文言詞喺英文介面照樣要見到原文。但要分開：原句保留，題幹嘅解釋性中文係漏譯。
2. 因為三科係**單語設計**：`m(s) = [s, s]`，英文欄由中文欄直接複製，唔係漏譯。18,626 處全部係咁。「修」佢等於毀咗中文科。
3. 每次動作之前跑 `npx tsx scripts/qbank/scan-en-cjk.mts` 對數，改完再跑一次。呢個掃描器本身就係為咗唔好再一刀切而寫。

## 19. Dr.Lee 英文

1. 翻譯，唔係刪。嗰句嘅中文係 utilitarianism 嘅定義本體，剷走就冇咗考核點。要保留原文就寫成 `the overall consequences an act actually produces（行為實際造成的整體後果）`。
2. `Largo (廣板)`、`鋅 / zinc` 係對照，**要科目首席決定**英文介面下保唔保留 —— 我唔會替你定。化學 54 處全部係呢一類。
3. 唔影響對比度。呢條問題嘅前提（`text-muted-foreground` 誤用）已經唔成立：實測 0 處。

## 20. Dr.Wong 數學

1. 因為會議紀錄數錯咗。實測 `batch2-uncovered.decisions.json` 得 **12 條**，全部 `pending`，`_meta.reviewer` 空白。
2. 「第二項」喺數學可以指多項式／數列嘅項 —— 所以 `check-posref` **唔會**掃數學解析嘅全文，只掃 `explanation` 欄再排除呢啲用法（`check-posref.mjs:35-36`）。要改嘅係**指住選項**嗰啲，要逐條睇。
3. 冇關係 —— 「48 份」呢個數字唔存在。12 張卡對應 12 個課題（`ma-arith-seq` … `ma-sig-fig`）。

## 21. Dr.Cheung 經濟

1. `economics-written-b2.json` · `ec_w2_03` · 兩個欄：`explanationEn` 有「企業家職能」、`markingSchemeEn` 有「企業」。⚠️ §5 對照表：`Entrepreneurship` 強制譯「企業家職能」，**唔可以**為咗清債改成「企業」。
2. `_reviewer-gate.mjs` 認 filesystem 簽名。代簽即係喺 `decisions.json` 寫一個唔係你嘅名 —— 咁份實名審批記錄就冇咗意義。憲章 §12 全條流程靠嗰個欄。
3. 經濟已經有 20 張卡上線（4 科各 20，合共 80 張）。零嘅係另外 **21 科**。優先次序要睇邊科題量大、學生多，呢個係內容決策。

## 22. Dr.Lau BAFS

1. BAFS 喺 745 處入面**零命中** —— 冇英文欄夾中文嘅債。
2. 清單 §A 已經逐科分開，每科一行簽名欄，可以直接剪去畀對應首席。
3. 課題→卡對應表未存在（同構標記零）。BAFS 要填就要先有 `archetype` 欄，而嗰個欄本身要先決定 optional（見 3.3）。

## 23. Dr.Ho 理科

1. 要你決定，唔係我。判斷點：`鋅 / zinc` 剷走中文之後題目仲通唔通？通 = 對照（可保留）；唔通 = 漏譯。化學 54 處（`contentEn` 30 ＋ `explanationEn` 24）。
2. 理科（物理／化學／生物）**0 張卡**。要補就行返 SENSEI 管線：出草稿 → `review-sensei-cards.mjs` → 真人逐張批 → `promote` → 人手加 import。
3. 因為英文介面應該一致。學生見到句子中間跳去中文，第一反應係「個站壞咗」，唔係「呢度係專有名詞」——45 號自修生自己都咁講（見 45.1）。

## 24. Dr.Patel SEN

1. ⚠️ **「30-40%」呢個數我冇來源，唔會寫入任何文件**（§8 禁虛構統計）。已落地嘅：OpenDyslexic 字體、防跳行閱讀尺、柔和呈現，全部喺 `A11yPanel`，自選、預設關。行距要加要改 CSS token，唔係加套件。
2. 同上，「82.4%」我冇來源。Pulse 本身未存在。真要做，避標籤嘅做法係：唔問「你係咪有困難」，只問「呢題有冇幫到你」，而且**唔記名、唔記次數、唔跨用戶聚合**。
3. 冇風險：一次 200ms opacity 轉換，唔係循環閃爍，`html.no-motion` 已覆蓋。⚠️ 但唔可以對外講「攔截光敏風險」（§16.D）。

## 25. Emily DNA

1. 實測：4 科（中文／經濟／英文／數學）各 20 張 = **80 張**；`data/subjects.ts` 有 **25 科** → **21 科 0 張**。
2. 而家做唔到 —— `archetype` 欄零命中。要有人先決定加唔加、optional 定 required（見 3.3）。
3. 12 張卡已經 keyed on 12 個課題 id，但 SENSEI 卡係用**自由文字 `subTopic`**（`data/sensei/math/reviewed/*.ts`），同題庫 `topicId` 唔係同一套 key。呢個先係真正要補嘅對應表。

## 26. Dedupe

1. 同構題係「同結構、唔同數值」—— 定義上就唔係重複。但 `validate-banks` 而家零同構概念，所以佢**唔會**誤殺，亦唔會幫到手。
2. `scan-en-cjk` 已經三分類（`CHINESE_SUBJECTS` 白名單），dedupe 唔掃語言欄，唔會撞。
3. `npm test` 有 `topic-registration.test.mts` ＋ `topic-entry-points.test.mts` 守孤兒課題，今日 953/953 全綠。

## 27. Bilingual

1. `i18n-guard.mjs:68`：同行寫 `// i18n-exempt:` 就豁免，要寫理由。唔係因為 745 —— 豁免係畀 SEO meta 同語言科考試內容。
2. `scan-en-cjk` 用 `identical` 旗自動分類（英文欄逐字等於中文欄 = 單語設計）。`i18n-guard` 本身唔掃題庫資料檔。
3. 冇關係，而且 `text-muted-foreground` 實測 0 處。

## 28. Ollie Content Ops

1. 逐檔數 `decisions.json` 入面 `status === 'pending'`：145 條 / 20 檔，其中 `*-written-b4` 17 檔 140 條，另外 `_demo-math` 3、`economics-floor` 1、`ths-floor2` 1。
2. 293 條全部係已上線題庫嘅解析文字 —— 機器重跑會改埋啲已經人手改過嘅嘢。**人手清**，一條一條。
3. 而家已經守住：`npm run qa` 第二閘 208 檔零新增。你日常改完跑 `npm run qa` 就夠。

## 29. Zara Design

1. 六處帶齊檔名＋題 id＋欄位，已經喺 `content-debt-2026-09-16.md` §C 列晒（`ec_w2_03` ×2、`hist_p2_35~38`）。
2. 統一嘅講法係「兩套色系各有用途」：站內莫蘭迪跟主題變數，導出 PNG 鎖定標準霓虹色（離開網站都睇得明）。要寫入 §14 先算數。
3. `#00F5D4` 喺 `owl.svg` **冇殘留**（六隻色全部莫蘭迪）。但 `globals.css:31` 四個 `--color-neon-*` 仲喺度，係畀導出卡用嘅，唔係殘留。

## 30. Dr.Yip 數學首席

1. 12 張對 12 個課題：`ma-arith-seq`（等差數列）、`ma-seq-pattern`、`ma-linear-fn`、`ma-locus`、`ma-variation`、`ma-indices`、`ma-factorisation`、`ma-coord-geom`、`ma-fn-composite`、`ma-polygon-angles`、`ma-trig-ratio`、`ma-sig-fig`。
2. 流程要先存在 —— `archetype` 欄而家唔存在。真要加：optional、由首席喺審題時填、加一個唯讀掃描器數覆蓋率（唔係加閘）。
3. 冇「48 份」呢樣嘢。12 張卡就係全部。

## 31. Quinn Data

1. 加咗之後可以分三種：做夠 `intendedSize` = 完成；做少過但有 `abandonedBefore` = 中途離開；根本冇紀錄 = 未開始。而家三種全部塌埋做一種。
2. `scripts/analytics/retention.mts` 檔頭自己寫明：只有 `dse_progress`（`AttemptRecord[]` 帶 timestamp），`user_sessions` 歷史上 0 行，所以佢量嘅係「做過題嘅人有冇返嚟」，唔係「開過網站嘅人」。而家 `SyncProvider.tsx:187` 已經 ping 緊，之後會有數，但**過去補唔返**。
3. 按週聚合唔公開人數 = 只出百分比、唔出分母。⚠️ 但分母細過某個數，百分比本身就識別得到人 —— 要定一個最小分母先出數。

## 32. Atlas

1. 「12 框架」喺憲章**搵唔到** —— 佢係 `/goal` 入面嘅目標，唔係憲章條文。所以「內容債係下游」呢個推論冇憲章依據。
2. 擴到 21 科要行 SENSEI 管線，每科要科目首席逐張批。80 張用咗 4 科，照呢個速度 21 科 = 420 張。
3. 今日實測：298 個課題，**100% 有 3 條或以上**（唔係 98.9%）。指令見文末 ⑧。

## 33. Mem

1. 兩個都可以，但成本唔同：`ab_group` 入表 = 新一欄綁 `user_id` 嘅行為資料，要 §16.E 簽名；本機 hash 分組 = 零新增雲端資料、零審批，但換部機會換組。**A/B 已經係 11-09 才決定嘅事**，唔使而家揀。
2. 唔使。`SESSION_SIZE` 係常數，唔係用戶屬性。
3. `startedAt` 擺本機就唔會新增任何綁 `user_id` 嘅欄 —— 同 `lib/restDay.ts` 檔頭嗰句「休息習慣唔上 server」同一條線。

## 34. Breaker

1. `PracticeSession.tsx:664-689` 已經將「出唔出下一題掣」同「Enter 推唔推進」收埋做同一個判斷，所以兩者唔會再分叉。迴歸靠 `npm test`。
2. `lib/__tests__/overflow-probe.test.mts` 有負向自測（造一個一定爆版嘅 case，確認探針捉到）—— 冇負向自測嘅探針會靜靜哋報 0。
3. 情緒溫度計嗰條路徑要造狀態：答錯難題 → 溫度計出現 → 未揀維度 → 撳 Enter → 應該唔換題。呢個係 `:668` 註釋講嘅原 bug。

## 35. Eva

1. `intendedSize` 記低開卷時揀咗幾題，`abandonedBefore` 記低喺第幾題走。74% 入面「揀咗 5 題做晒 5 題」同「揀咗 10 題做 5 題走人」就分得開。
2. 純 `localStorage`，唔上雲 → 零 Edge request。上雲先要簽。
3. 而家計到嘅係「做過題嘅人有冇隔日再做題」（`analytics:retention`）。「開過網站有冇返嚟」要靠 `user_sessions`，2026-09-11 之後先有數。

## 36. Nova Judge

1. **佢唔紅。** 今日 953/953 全綠。`charter-review-dates.test.mts` 守嘅係：覆檢日要有對應紀錄，而且 `where` 引用嘅 docs 檔要真係存在。
2. 條文寫喺 §1（程序條文），草案 `87cdeaa` §四已經寫好：「裁決同副署之間嘅空窗唔可以超過 7 日」。提醒靠 `charter-review-dates.test.mts` 加一條到期檢查。
3. 改 `charter.md` 文末嗰段簽署紀錄（`⬜ 待 Brian 副署` 嗰行），同日出 amendment 檔。

## 37. Leo2

1. `lib/__tests__/rest-day.test.mts`。守嘅係「休息日照樣有一條做題嘅路，撳落去乜都唔會發生」——唔會彈確認、唔會記低、唔會下次提。
2. 因為連續打卡本身就係禁嘅（`no-daily-streak.test.mts`）。休息日顯示「你斷咗」會將一個自己揀嘅休息變成一次失敗。
3. 「一鍵舒適模式」係衍生狀態。`comfortOn` 計入嘅話，今日已經開住舒適模式嘅學生會因為某個子掣係 0 而突然見到總掣變「關」——一個純補掣嘅改動唔應該令任何人已有嘅狀態顯示變樣（§7.2 尾段已記低同一個推導）。

## 38. Sam2

1. `isomorphic_group` 同 `archetype` 答唔同問題：前者標「呢幾條係同一組同構題」，後者標「呢條用邊個框架」。A/B 閉環要嘅係前者。**兩個都未存在，而且 A/B 本身 11-09 先決定** —— 而家唔應該落欄。
2. 生成唔到 —— 冇同構標記就冇對應關係，98.9%／100% 呢個數字講嘅係「每個課題有幾多條題」，同「邊幾條係同構」冇關。
3. 11-09 之前要準備嘅嘢已經寫晒喺 `docs/iso-loop-design-2026-11-09.md`，唔使再分批。

## 39. Ada2

1. 加一行 `grep -q supermarket next.config.ts && exit 1` 落 CI。但講真，冇人會再加返 —— 真正守住佢嘅係憲章 §8.1.1 存檔咗剷除理由。
2. 已經寫咗：`docs/dev-stale-css-root-cause-2026-09-16.md`，有實測表（380MB vs 690MB）。README 要唔要指返過去，我可以加。
3. 見 4.1 —— 刪 `.next/dev/cache` 保住 build cache，慳好多重 build 時間。

## 40. Mimi UX

1. 唔係私隱條款禁，係憲章禁：§8 禁虛構統計、§16.E 約束 3 禁跨用戶匯總比較。即係話就算個數字係**真**嘅，跨用戶聚合出嚟嘅比較數字一樣唔做。
2. `lib/quietMode.ts` 已有 `dse_quiet_mode`，而且**刻意冇入 `lib/sync.ts`**（`quiet-mode.test.mts:44` 守住）。同一鍵舒適聯動要跟 37.3 嗰個衍生狀態做法。
3. Pulse 未存在，`dse_pulse_last_shown` 零命中。要做嘅話：每日最多一次係為咗唔變成騷擾，而且 key 要純本機。

## 41. Sean SEO

1. `components/Seo/ArticleJsonLd.tsx` 已經處理咗：JSON-LD 聲稱嘅日期必須**同時可見**（檔頭第 10 行）。憲章唔使為佢開豁免 —— 一個只喺機器可讀層聲稱、人眼睇唔到嘅日期，先係要禁嘅嘢。
2. `theme_color` ／ `background_color` 都係 `#F4F0EA`，冇殘留。但 `#00726C` 仍然係活嘅 `--color-accent-strong`（111 處用緊），唔係要清嘅嘢。
3. 冇直接 SEO 影響。PWA 圖示影響嘅係加到主畫面之後個 icon，唔係搜尋排名。

## 42. Sam Growth

1. 量唔到中途離開 = 量唔到「10 題卷係咪太長」。而家可以計嘅留存係 `analytics:retention` 嘅 Day-2（只限做過題嘅人）。
2. Pulse 未存在。真要計：有呀 / 仲好 / 未係 → helpful_rate = 「有呀」÷ 總數，按週出百分比，唔出人數（見 31.3 嘅最小分母問題）。
3. 293 條位置式引用會令解析指錯選項 —— 學生照住解析去對答案就會對錯。P1 邊緣生本身信心最薄，一次指錯就足以令佢唔信個平台。

## 43. Fiona P1

1. 選項每次呈現都洗牌，所以解析寫「第二項」必然指向另一個選項。唔係風格問題，係**畀錯答案**。
2. 休息日護盾唔顯示斷纜，因為「斷纜」本身會製造一個自己揀嘅休息變成失敗嘅感覺（`restDay.ts` 檔頭寫明 ❌ 真鎖 ✅ 摩擦）。
3. 安靜模式收起段位／EXP／溫習時數。對 P1 邊緣生嚟講，見到自己嘅段位低過人係一個唔關學習事嘅打擊 —— 所以佢係自選、可以整層收埋（§8.1 約束 4）。

## 44. Ray Teacher

1. §8 禁嘅係**老師平台**（`classes` / `enrollments` / `question_events` 已刪除，維持刪除）。`Resend` 全 repo 零命中 —— 冇裝過，亦唔會裝（§5 嚴禁新增套件 ＋ §5 成本死鎖）。
2. 代簽會令實名審批記錄失去意義（見 21.2）。老師睇：一個唔肯代簽嘅系統，先值得信佢張「已審核」標。
3. 替代品已經存在：`/dashboard/report`「我嘅溫書地圖」——純客戶端，學生自己下載 PNG，自己揀畀唔畀老師睇。冇老師登入、冇 server 數據、冇跨學生比較。

## 45. Self 自修生

1. 因為題庫入面 685 條題嘅英文欄夾住未翻譯嘅中文（745 處）。**唔係 bug，係內容債** —— 所以先要各科首席簽名決定點清。
2. 因為「做 1 題」係一個做得到嘅邀請，「致命錯因」係一個判斷。§7 大愛紅線禁嘅正正係打擊自信嘅字眼 —— 平台可以講你發現咗乜，唔應該講你有幾致命。
3. `Shift + F` 令題目卡以外嘅嘢淡到 0.25，hover 返嚟就全亮。對 ADHD 學生實際：唔使用意志力去忽略周邊。**自選、預設關**，而且手機冇 Shift，所以 `A11yPanel` 入面有同一個掣。

---

## 我冇答、亦唔應該由我答嘅

| 問題 | 點解 |
|---|---|
| 1.1 即日復原定雙簽 | 憲章修訂要 Brian ＋ Yuna 雙簽，我唔可以代任何人簽或者代決定 |
| 19.2、23.1 雙語對照保留唔保留 | 科目首席嘅內容判斷，745 處點清係佢哋簽名嘅嘢 |
| 5.1 QualityBadge 定清債先 | 產品次序，要founder 拍板 |
| 24.1、24.2 SEN 百分比 | 我冇來源，§8 禁虛構統計 —— 有出處畀我我就寫入去 |
| 6.2 next 16.3.2 upstream break change | 我冇網路，查唔到 changelog；只答得到實測到嘅嘢 |
| 13.1 iOS Add to Home Screen | 未測過，§16.C 唔准預填 |

---

## 重跑成份文件

```bash
node -p "require('./package.json').scripts.dev"                  # §0-1
grep -rn "text-muted-foreground" app components | wc -l          # §0-2
npm test                                                          # §0-3 · 953/953
npx tsx scripts/qbank/scan-en-cjk.mts                            # §0-5 · 745/122/18626
git status -sb                                                    # §0-7
grep -rn "AnonymousPulse\|dse_pulse\|ab_group\|archetype" . \
  --include="*.ts" --include="*.tsx" | grep -v node_modules      # §0-8/9 · 零命中
npm run qa                                                        # 10 閘
npm run sync:conflict-repro                                       # 4/4 · 36/9
node scripts/qbank/check-posref.mjs --banks                       # 293 / 208 檔
npm run qbank:charscan -- --all                                   # 6 處 / 2 檔
```

⑧ 課題覆蓋率（§0-6、32.3）：

```bash
npx tsx -e "
import {subjects} from './data/subjects'
import {getSubjectQuestions} from './data/questions'
let tot=0, ge3=0
for (const s of subjects) {
  const m = new Map<string, number>()
  for (const q of getSubjectQuestions(s.id) as any[]) m.set(q.topicId ?? q.topic, (m.get(q.topicId ?? q.topic) ?? 0) + 1)
  for (const [, n] of m) { tot++; if (n >= 3) ge3++ }
}
console.log(tot, ge3, (ge3/tot*100).toFixed(1) + '%')"
```
