// The platform is 100% free, for everyone. There are no tiers, paywall, attempt
// caps, school whitelist or premium gating anywhere — every subject is fully
// unlocked for every visitor, signed in or not. (Google sign-in still exists, but
// only to sync progress across devices; it grants no extra access.)
//
// This module used to be the entitlements/pricing source of truth. All that is
// gone; the only thing the practice runner still needs from here is how many
// questions to draw per run.

export const SESSION_SIZE = 20
